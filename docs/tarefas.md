# Tarefas — BomberQuiz

> Atualize este arquivo ao final de cada sessão de trabalho não-trivial.

## Realizadas

- [x] 2026-05-28 — Estrutura inicial do projeto: `CLAUDE.md`, `docs/requisitos.md`, `docs/tarefas.md`, `docs/decisoes.md`.
- [x] 2026-05-28 — Visão geral, modelo de negócio (gratuito + assinaturas), atores (Administrador, Cliente, Parceiro) e lista ilustrativa de matérias do TAP em `docs/requisitos.md`.
- [x] 2026-05-28 — Hierarquia de conteúdo definida: Eixo Temático → Matéria → Pergunta. Cadastros mapeados. Decisões registradas em ADR-0003 e ADR-0004.
- [x] 2026-05-28 — Cadastro de usuário (nome, e-mail único, WhatsApp, DOB, sexo, senha, avatar opcional), whitelist de administradores (máx. 5), e funcionalidade "Doar assinatura" especificadas. ADR-0005, 0006 e 0007 registrados.
- [x] 2026-05-28 — Stack técnica definida (Bun + Hono + Drizzle + Postgres/Neon + Better-Auth no backend, Vite + React + Tailwind + shadcn + PWA no frontend; Fly.io + Cloudflare Pages; Mercado Pago + WhatsApp Cloud API + Resend + R2 como integrações). Arquitetura hexagonal documentada em `docs/arquitetura.md`. ADR-0008 a ADR-0012 registrados.
- [x] 2026-05-28 — Convenção de RFs definida (6 módulos, formato `<MODULO>-RF-NNN`, arquivos separados em `docs/rf/`). Índice criado em `docs/requisitos.md`.
- [x] 2026-05-28 — Módulo 1 (Autenticação e cadastro) — rascunho com AUTH-RF-001 a AUTH-RF-011 em `docs/rf/auth.md`. 5 pendências identificadas (AUTH-P-01 a AUTH-P-05).
- [x] 2026-05-28 — Pendências do Módulo 1 resolvidas: idade mínima 18 anos; sexo (M/F/Prefere não informar); recuperação só por e-mail; sem OTP de WhatsApp; avatar como URL em Cloudinary. ADR-0013 registrado.
- [x] 2026-05-28 — Módulo 2 (Perfil e papéis) — rascunho com PROF-RF-001 a PROF-RF-013 em `docs/rf/profile.md`. Decisões: sessão única estilo Netflix (ADR-0014, ajustes em AUTH-RF-005 CA-2 e AUTH-RF-008 CA-4), dois fluxos de saída de conta — desativar reversível + excluir com anonimização LGPD (ADR-0015), admin não edita dados pessoais de outros usuários, portabilidade LGPD sob demanda via e-mail (sem endpoint no MVP). 4 pendências identificadas (PROF-P-01 a PROF-P-04).
- [x] 2026-05-28 — Pendências do Módulo 2 resolvidas: sem auto-exclusão de contas desativadas no MVP (P-01); detalhe admin de usuário com resumo financeiro via novo PROF-RF-014 (P-02); cooldown anti-takeover de 30 dias entre trocas de e-mail e 7 dias bloqueando exclusão pós-troca (P-03); hash do e-mail anonimizado com sal global em env (P-04).
- [x] 2026-05-28 — Módulo 3 (Conteúdo admin) — rascunho com CONT-RF-001 a CONT-RF-016 em `docs/rf/content-admin.md`. Decisões: estrutura mínima da pergunta no MVP — 4 alternativas fixas, 1 correta, justificativa obrigatória, fonte oficial em texto livre opcional, 1 imagem opcional via R2 (ADR-0016); admin publica direto, parceiro entra em fila `pending_review` com aprovação/rejeição (motivo obrigatório); soft-delete `archived` em eixos/matérias/perguntas preservando estatísticas; reset opcional de estatísticas em edição via flag `reset_stats`. 4 pendências identificadas (CONT-P-01 a CONT-P-04).
- [x] 2026-05-28 — Pendências do Módulo 3 resolvidas: nível de dificuldade via job diário às 00:00 com bandas unrated/easy/medium/hard (novo CONT-RF-017); sem versionamento automático de perguntas + hard-delete permitido apenas quando `total_answers=0` (CONT-RF-012 atualizado); admins permanecem todos iguais (mantém ADR-0005); reset total de estatísticas confirmado.
- [x] 2026-05-28 — Módulo 4 (Conteúdo parceiro) — rascunho com PART-RF-001 a PART-RF-008 em `docs/rf/content-partner.md`. Decisões: parceiro cadastra livre em qualquer matéria ativa (sem especialidade); edição de própria publicada devolve a pergunta para `pending_review`; exclusão restrita a `draft`; parceiro vê estatísticas e histórico de revisão das próprias; painel/dashboard com agregados motivacionais. 4 pendências identificadas (PART-P-01 a PART-P-04).
- [x] 2026-05-28 — Pendências do Módulo 4 resolvidas: limite de 50 rascunhos confirmado (P-01); notificação ao parceiro via badge no app (`unread_review_events`) + e-mail transacional Resend (P-02), formalizada em CONT-RF-015/016 e PART-RF-007/008; parceiro só vê as próprias perguntas (P-03), divisão de trabalho fica externa ao sistema; rubrica operacional inicial criada em `docs/rubrica-aprovacao.md` (P-04).
- [x] 2026-05-28 — Módulo 5 (Quiz cliente) — rascunho com QUIZ-RF-001 a QUIZ-RF-009 em `docs/rf/quiz.md`. Decisões: 3 modos no MVP (simulado TAP respeitando `tap_weight`, livre por matéria, livre por eixo); cronômetro opcional com tempo total (3 min/questão default, ajustável 1–5 min); justificativa configurável (após cada / só no final); quiz online-only e efêmero (sem pause/resume); snapshot da pergunta no momento do sorteio para imunidade a edições futuras; reset total de estatísticas pelo cliente preservando histórico de quizzes; bloqueio HTTP 402 quando sem assinatura/trial ativos. 7 pendências identificadas (QUIZ-P-01 a QUIZ-P-07).
- [x] 2026-05-28 — Pendências do Módulo 5 resolvidas: assimetria explícita de estatísticas (finished/expired contam não-respondidas como erro; abandoned preserva só o que foi respondido) + auto-abandono após 24h sem atividade (QUIZ-P-02); reset seletivo descartado, total mantido (QUIZ-P-03); UI de cronômetro com toggle/switch principal formalizada em QUIZ-RF-001 (QUIZ-P-06); leaderboard descartado em favor de novo QUIZ-RF-010 — evolução temporal própria do usuário (QUIZ-P-07). P-01/04/05 mantidas conscientemente como pós-MVP.
- [x] 2026-05-28 — Módulo 6 (Assinaturas e cortesias) — rascunho com SUB-RF-001 a SUB-RF-015 em `docs/rf/subscriptions.md`. Decisões: trial automático de 7 dias após verificação de e-mail (único por usuário); 4 planos (mensal/trimestral/semestral/anual) configuráveis pelo admin; cobrança via PIX, saldo MP e cartão (cartão +10%, parcelável em até 3×); sem recorrência automática no MVP — TAP é cíclico, lembretes D-7/D-3/D-1 + e-mail final no D-0; cortesia (renomeado de "doação") como funcionalidade de primeira classe com 2 categorias (parceria/demonstracao), limite de 10/mês por admin e revogação com motivo obrigatório; cupom de desconto incluído no MVP (SUB-RF-013/015) para marketing inicial; reembolso automático em 7 dias atendendo CDC art. 49 (SUB-RF-014); comprovante via link MP; painel financeiro separa receitas de cortesias (ADR-0006); cálculo de `access_status` consolida trial+paga+cortesia (consumido por QUIZ-RF-009 e PROF-RF-014). Todas as 6 pendências iniciais resolvidas.
- [x] 2026-05-28 — Renomeação "doação" → "cortesia" propagada em toda a documentação (subscriptions.md, profile.md, requisitos.md, decisoes.md, arquitetura.md). Schema renomeado: tabela `courtesies`, `source=courtesy`, `courtesy_id`. ADR-0006 atualizado.
- [x] 2026-05-28 — Revisão pré-implementação dos 8 itens identificados: (1) schema do `audit_log` documentado em `arquitetura.md`; (2) formato padronizado de resposta de erro `{ error: { code, message, details?, fields?, request_id } }`; (3) rate limit baseline 60 req/min por IP + 120 req/min por user; (4) estratégia de domínios em duas fases (provisórios `pages.dev`/`fly.dev` → próprios `*.bomberquiz.com.br` no lançamento) com config-driven URLs; (5) Resend confirmado como provedor de e-mail; (6) WhatsApp adiado conscientemente (sem canal ativo no MVP); (7) Política de privacidade + Termos como bloqueio explícito de lançamento (não da implementação); (8) matérias do TAP cadastráveis em runtime — ativar/desativar via CONT-RF-008 absorve o ciclo anual do edital (labels de UI: "Desativar matéria"/"Reativar matéria").

- [x] 2026-06-17 — Documento de síntese [`docs/visao-geral.md`](visao-geral.md) criado: partes do sistema, dependências externas, módulos funcionais, modelo de dados, aspectos transversais e bloqueios críticos.
- [x] 2026-06-17 — **Módulo 7 — Geração de Questões por IA** especificado em [`docs/rf/ai-generation.md`](rf/ai-generation.md): 9 RFs (AIGEN-RF-001..009), 4 ADRs (0021–0024), 9 endpoints adicionados ao [`api.md`](api.md), módulo adicionado ao índice de [`requisitos.md`](requisitos.md). Escopo: upload de 2 PDFs (prova de referência + material), processamento assíncrono via Claude API, revisão questão-a-questão, aprovação direta para `published`.
- [x] 2026-06-17 — **Otimizações de custo/performance do M7** especificadas: (1) cache de exemplos de prova de referência em tabela `ai_reference_exams` indexada por SHA-256 — elimina reprocessamento do mesmo arquivo em jobs futuros (ADR-0025, atualização de AIGEN-RF-004 CA-4); (2) prompt caching via `cache_control: {type: "ephemeral"}` no bloco `[system + exemplos]` — reduz custo de tokens em ~90% para chunks subsequentes em jobs multi-parte (ADR-0026, atualização de AIGEN-RF-004 CA-5). Modelo de dados atualizado (`ai_reference_exams`, campos `reference_exam_id` e `cached_tokens` em `ai_generation_jobs`). `docs/visao-geral.md` atualizado (7 módulos, ~80 endpoints, 3 novas tabelas, ADRs 0001–0026).

- [x] 2026-05-29 — Segunda rodada de revisão pré-implementação (10 lacunas avaliadas e tratadas): **(C)** reconciliado o "Modelo de dados" de `arquitetura.md` com os RFs (corrigido `subscription_donations`→`courtesies`, `quizzes`→`quiz_sessions`, tabelas faltantes `subscription_plans`/`coupons`/`user_access`/`sessions`, campos de `users`); **(B1)** ADR-0017 — jobs via scheduler in-process + seção "Jobs agendados" em `arquitetura.md`; **(B2)** ADR-0018 — fronteira Better-Auth × custom (com spike obrigatório no bootstrap); **(B3)** canal de suporte = e-mail (`SUPPORT_EMAIL`, config-driven); **(B4)** ADR-0019 — NFS-e fora do escopo do MVP; **(B5)** ADR-0020 — backup PITR Neon + dump lógico para R2; **(D1)** `GET /plans` tornado público (SUB-RF-001); **(D2)** expurgo de IP/user-agent do `audit_log` após 12 meses + nota para política de privacidade; **(D3)** catálogo consolidado de e-mails transacionais em `arquitetura.md`; **(D4)** simulado TAP — prova real tem 50 questões; teto defensivo de 60 questões na soma dos `tap_weight` (QUIZ-RF-001 E-6).

## A realizar — Próximos passos

- [x] ~~Definir o tipo de aplicação~~ — PWA web mobile-first (ADR-0010).
- [x] ~~Definir gateway de pagamento~~ — Mercado Pago (ADR-0012).
- [x] ~~Definir forma de armazenamento~~ — Postgres no Neon, via Drizzle (ADR-0009).
- [x] ~~Esboçar fluxos principais detalhados~~ — coberto pelos CAs de cada RF nos arquivos `docs/rf/*.md`.
- [ ] **Escrever os módulos de RF restantes:**
  - [x] Módulo 1 — Autenticação e cadastro (`docs/rf/auth.md`).
  - [x] Módulo 2 — Perfil e papéis (`docs/rf/profile.md`).
  - [x] Módulo 3 — Conteúdo admin (`docs/rf/content-admin.md`).
  - [x] Módulo 4 — Conteúdo parceiro (`docs/rf/content-partner.md`).
  - [x] Módulo 5 — Quiz (`docs/rf/quiz.md`).
  - [x] Módulo 6 — Assinaturas e cortesias (`docs/rf/subscriptions.md`).
- [x] ~~Resolver pendências do Módulo 1 (AUTH-P-01 a AUTH-P-05).~~ Concluído.
- [ ] Detalhar modelo de dados (schema Drizzle) a partir do esboço em `docs/arquitetura.md`.
- [x] 2026-05-29 — **Contrato da API** consolidado em [`api.md`](api.md): inventário dos endpoints dos 6 módulos (método, rota, papel, RF) + convenções transversais (auth por cookie, paginação, envelope de erro, versionamento, idempotência, datas/centavos). A spec OpenAPI **canônica** continua sendo gerada do Zod no backend (ADR-0008) — `api.md` é a planta e o checklist de conformidade.
- [ ] Gerar a spec OpenAPI de fato no `bomberquiz-api` (`@hono/zod-openapi` + Scalar), conforme a planta em `api.md`, e configurar o download/geração do cliente no CI do `bomberquiz-web`.
- [ ] Definir mecanismo de geração do cliente HTTP no frontend a partir da spec OpenAPI.
- [x] ~~Resolver questões em aberto na seção final de `docs/requisitos.md`~~ — todas as questões que estavam na lista original foram resolvidas durante a redação dos Módulos 1–6. Pendências específicas residuais ficam nos rodapés dos arquivos `rf/*.md` (códigos `<MOD>-P-NN`).
- [x] ~~Listar as matérias efetivas do TAP~~ — **não-aplicável como tarefa prévia**. O cadastro acontece naturalmente via UI quando o admin estiver no ar (CONT-RF-002/006), e o ciclo do edital (matérias entram/saem a cada ano) é absorvido pela mecânica de ativar/desativar matéria (CONT-RF-008 CA-5).
- [x] ~~Criar os dois repositórios no GitHub~~ — `bomberquiz-api` e `bomberquiz-web` criados (`github.com/aetdesenvolvimentoweb/bomberquiz-api` e `.../bomberquiz-web`).
- [x] 2026-06-26 — Bootstrap do `bomberquiz-api` (estrutura de pastas hexagonal, `package.json` Bun, `tsconfig.json`, `src/index.ts` com `Bun.serve`, `src/http/app.ts` com Hono + `@hono/zod-openapi`, `src/config/env.ts` com validação Zod, `/health` + `/openapi.json` + `/docs` funcionando). Dependências: `hono`, `@hono/zod-openapi`, `zod`. Drizzle, Better-Auth e demais ficam para próximas etapas.
  - [ ] **Spike Better-Auth (ADR-0018):** validar que os hooks (`before/after` signIn/signUp, `databaseHooks`) suportam sessão única, lockout exponencial, cooldown de troca de e-mail, alerta ao e-mail antigo, versionamento de consentimento e anonimização LGPD. Onde não suportar, implementar sobre as tabelas do Better-Auth via Drizzle.
  - [ ] **Scheduler in-process (ADR-0017):** wiring dos 5 jobs (dificuldade, lembretes, expiração de quiz, purga de sessões, dump de backup) em `infra/scheduler/`, com horários em env e idempotência.
  - [ ] **Backup (ADR-0020):** job de `pg_dump` → R2 + runbook de restauração no repo.
- [x] 2026-07-07 — Bootstrap do `bomberquiz-web` (Vite + React + React Router, Tailwind + shadcn/ui, TanStack Query, vite-plugin-pwa, Vitest + RTL) com o frontend do que já existe na API: cadastro, verificação de e-mail, reenvio de verificação, login, logout, esqueci/redefinir senha, hidratação de sessão (`GET /me`) com reaceite de consentimento, perfil (ver/editar dados, trocar senha, trocar e-mail + confirmação, desativar conta, excluir conta LGPD). Cliente HTTP tipado gerado do `openapi.json` via `openapi-typescript`/`openapi-fetch`. Fora de escopo por falta de endpoint: avatar, reativação de conta, admin/parceiro/conteúdo/quiz/assinaturas. Zustand (ADR-0010) ainda não instalado — sem estado de UI que o justifique nesta fatia.
  - **Proxy do Vite dev server adicionado** (`web/vite.config.ts` § `server.proxy`) para `/auth`, `/me`, `/admin`, `/plans`, `/quizzes`, `/webhooks`, `/api`, `/health` → `http://localhost:3000`: necessário porque o cookie de sessão usa `SameSite=Lax` fora de produção (`api/src/infra/auth/better-auth.ts`), que o navegador não envia em fetch/XHR cross-site (localhost:5173 → localhost:3000) fora de navegação top-level. Com o proxy, o front enxerga tudo como same-origin. `VITE_API_BASE_URL` vazio (`.env.example`) ativa esse modo; em produção deve ser a URL absoluta do backend implantado.
  - **🐛 Dois bugs reais encontrados no `bomberquiz-api` durante a verificação end-to-end no navegador** (não corrigidos — fora do escopo desta tarefa, que era só o frontend):
    1. [x] ~~`application/auth/login.usecase.ts` não checa o `status` da `Response` de `auth.api.signInEmail(...)`~~ — **corrigido em 2026-07-08**, ver entrada abaixo.
    2. Tabela `verifications` **fica sempre vazia** (checado direto no Neon de dev: 9 usuários cadastrados, 0 linhas em `verifications`) — o token de verificação de e-mail (AUTH-RF-002) não está sendo persistido pelo Better-Auth nesta configuração, então nenhum usuário consegue de fato verificar o e-mail pelo fluxo normal hoje. Investigar a config de `emailVerification` em `better-auth.ts` / possível causa no spike ADR-0018. **Ainda aberto.**
- [x] 2026-07-07 — Deploy do `bomberquiz-api` configurado: Fly.io (`bomberquiz-api` produção + `bomberquiz-api-staging`) + Neon. Detalhes na entrada abaixo.
  - [x] 2026-07-08 — Configurar deploy do `bomberquiz-web`: Cloudflare Pages (produção). Ver entrada dedicada abaixo. Preview de `staging` ainda pendente (nenhum dos dois repos tem branch `staging` criada ainda).
- [x] ~~Validar provedor de e-mail~~ — **Resend** confirmado (ADR-0012 atualizado).
- [ ] Validar provedor de WhatsApp (Cloud API vs Z-API) — adiado conscientemente; WhatsApp não é canal ativo no MVP (só contato declarado). Reavaliar quando virar canal ativo.
- [ ] **🚩 Bloqueio de lançamento público:** Redigir Política de Privacidade + Termos de Uso (LGPD). Beta privado pode rodar com versão preliminar; contratar advogado especializado para revisão antes da abertura pública. Versionar como `docs/legal/privacidade-vN.md` e `docs/legal/termos-vN.md` (sistema já tem `consent_version` e reaceite em PROF-RF-006).
- [ ] Especificar fluxo técnico de verificação de e-mail (envio, expiração de token, reenvio).
- [ ] **🚩 Pendência jurídica/fiscal (ADR-0019):** definir CNPJ e regime tributário; decidir emissão de NFS-e (manual no início vs. integração com emissor). Reavaliar antes/logo após o lançamento público. Pré-requisito também para o WhatsApp Cloud API (exige CNPJ/KYC).
- [ ] Definir `SUPPORT_EMAIL` real e criar a caixa de suporte (ex.: `suporte@bomberquiz.com.br` na Fase 2; um endereço provisório na Fase 1/beta). **→ plano de execução concreto na seção "Próximo passo — Domínio e e-mail" abaixo.**
- [x] 2026-07-07 — **Deploy do `bomberquiz-api` no Fly.io + Neon.** `Dockerfile`, `fly.toml` (app `bomberquiz-api`) e `fly.staging.toml` (app `bomberquiz-api-staging`) criados; região `gru`. Pipeline de CI/CD em `.github/workflows/deploy.yml`: push em `main` → deploy produção, push em `staging` → deploy staging (etapa atual: `typecheck` → `deploy`; `lint`/`unit`/`integration` ficam como `TODO` até existirem no repo — ver pendência abaixo). `FLY_API_TOKEN` (org-wide) cadastrado como secret no GitHub via `gh secret set`. `release_command = "bun run db:migrate"` roda as migrations do Drizzle a cada deploy. Ambos os apps escalados para **1 máquina cada** (em vez do padrão HA de 2/app do Fly) para caber no free tier — ver ADR-0027. URLs no ar: `https://bomberquiz-api.fly.dev` e `https://bomberquiz-api-staging.fly.dev` (`/health` OK nos dois).
  - [x] ~~Separar banco de staging do de produção~~ — obsoleto: ambiente de staging aposentado em 2026-08-03 sem nunca ter sido usado de fato (ADR-0037, ver entrada abaixo).
  - [x] 2026-07-08 — `WEB_ORIGIN` de produção atualizado para `https://bomberquiz-web.pages.dev` (`fly secrets set --app bomberquiz-api`). CORS e `trustedOrigins` do Better-Auth confirmados via preflight real.
  - [x] ~~Atualizar `WEB_ORIGIN` do app `bomberquiz-api-staging`~~ — obsoleto: app destruído em 2026-08-03 (ADR-0037, ver entrada abaixo).
  - [ ] **Adicionar `lint` e testes ao pipeline de CI do `bomberquiz-api`** assim que existirem (`bun run lint`, `bun test tests/unit`, `bun test tests/integration`) — hoje `package.json` só tem `typecheck`, e `tests/{unit,integration,e2e}` só têm `.gitkeep`.
- [x] 2026-07-08 — **Deploy do `bomberquiz-web` no Cloudflare Pages.** Projeto `bomberquiz-web` criado via `wrangler pages project create` (branch de produção `main`). Pipeline de CI/CD em `.github/workflows/deploy.yml`: `typecheck` → `test` (unit) → `build` (com `VITE_API_BASE_URL` fixado por branch: `https://bomberquiz-api.fly.dev` em `main`, `https://bomberquiz-api-staging.fly.dev` em `staging`) → `wrangler pages deploy` via `cloudflare/wrangler-action@v3`. Secrets `CLOUDFLARE_API_TOKEN` e `CLOUDFLARE_ACCOUNT_ID` cadastrados no GitHub via `gh secret set`. Adicionado `public/_redirects` (`/* /index.html 200`) para fallback de SPA nas rotas do `react-router-dom`. `WEB_ORIGIN` do backend produção atualizado para apontar para a URL real. URL no ar: `https://bomberquiz-web.pages.dev` — validado: build, testes unitários, deploy, `/`, `/login` e `/perfil` (deep link) retornando 200, preflight CORS reconhecendo a origem com `credentials: include`.
  - [x] ~~Criar branch `staging` em `bomberquiz-web` e `bomberquiz-api`~~ — decisão invertida: em vez de criar a branch que nunca existiu, o ambiente de staging foi aposentado (2026-08-03, ADR-0037, ver entrada abaixo).
  - [ ] Validação manual de login/cadastro/perfil no ar contra a API de produção (cadastro real, ver cookie de sessão no DevTools) — pendente de confirmação do usuário.
  - [ ] Adicionar `lint`, testes e2e (Playwright) ao pipeline de CI do `bomberquiz-web` quando existirem — hoje só há testes unitários (Vitest/RTL).
- [x] 2026-07-08 — **Validação end-to-end em produção** (`bomberquiz-web.pages.dev` + `bomberquiz-api.fly.dev`): cadastro, login e exibição de perfil confirmados funcionando pelo usuário. Três pontos identificados e tratados:
  1. **Dados de teste no banco de produção** — não há suíte automatizada gravando no banco (nem `web/` nem `api/` têm testes que fazem chamada de rede real); a origem foi validação manual via curl em sessão anterior (usuário "curl test" ainda no banco). Reforça a pendência já registrada (ADR-0027) de separar banco de staging/teste do de produção — sem isso, toda validação manual polui dados reais. Usuário ficou responsável por limpar as contas de teste (`curl test` e a `qa-login-fix-test@example.com` criada na validação abaixo).
  2. [x] **Resend não enviava e-mail de verificação** — causa: `EMAIL_FROM` apontava para `noreply@bomberquiz.com.br`, domínio ainda não registrado (Fase 2 do plano de domínios, pendente). Corrigido temporariamente trocando `EMAIL_FROM` para o sender de sandbox `BomberQuiz <onboarding@resend.dev>` via `fly secrets set --app bomberquiz-api`. **Limitação conhecida:** sandbox do Resend só entrega de fato para o e-mail dono da conta Resend — usuários reais só vão receber e-mail de fato após registrar `bomberquiz.com.br` e verificar o domínio no Resend. Reavaliar junto da Fase 2 de domínios.
  3. [x] **Login sem confirmação de e-mail retornava 200** — bug já registrado na entrada de 2026-07-07 acima, corrigido agora: `login.usecase.ts` passou a checar `betterAuthResponse.ok`/`status` e lança `EmailNotVerifiedError` explicitamente (classe que já existia em `user.errors.ts` mas não era usada nesse fluxo); `auth.routes.ts` trocou o match frágil por texto (`message.includes("EMAIL_NOT_VERIFIED")`) por `instanceof EmailNotVerifiedError`. Validado em produção: cadastro de conta sem verificar e-mail + tentativa de login agora retorna `403 { code: "email_not_verified" }` sem `set-cookie`, como esperado (AUTH-RF-004 E-2). Deploy via push direto em `main` (commit `740d070`).
- [x] 2026-07-09 — **Suíte de testes do `bomberquiz-api` expandida** (unit/integration/e2e, `bun:test`, padrão AAA, atenção a casos de borda): cobertura subiu de ~48%/55% para **84%/95%** (funções/linhas) nos módulos Auth e Perfil — únicos implementados até agora nos Módulos 3–7 (conteúdo/quiz/assinaturas/IA) segue sem código, então sem cobertura aplicável. 92 testes no total (34 unit, 5 integration, 53 e2e). A infra de teste (Postgres via Docker, isolamento por transação/truncate, stub da API Resend) já existia e funcionava desde o bootstrap — o trabalho foi de cobertura, não de setup. Script `test:coverage` (e variantes por camada) adicionado ao `package.json` usando o coverage nativo do Bun. Três bugs reais encontrados e corrigidos no processo:
  1. [x] Validação Zod retornava **HTTP 400** em vez de **422** (`@hono/zod-openapi` usa 400 por padrão; o envelope de erro documentado acima em "Formato padronizado de resposta de erro" exige 422 + `fields[]`). Corrigido com um `defaultHook` global (`src/http/lib/validation-hook.ts`) aplicado às 3 instâncias de `OpenAPIHono` (`app.ts`, `auth.routes.ts`, `me.routes.ts`).
  2. [x] Lista de senhas comuns (SecLists top-10k) **nunca existiu no repo** — `password-strength.ts` sempre caía no fallback de lista vazia; a checagem de senha comum estava inerte em produção desde sempre. Arquivo baixado de `SecLists/Passwords/Common-Credentials/10k-most-common.txt` e commitado em `src/lib/top-10k-passwords.txt`.
  3. [x] **🐛 "Esqueci minha senha" nunca funcionou em produção** — `forgot-password.usecase.ts` chamava `/api/auth/forget-password` (endpoint inexistente no Better-Auth; o correto é `/api/auth/request-password-reset`). O 404 era engolido por um `.catch(() => null)`, então nenhum e-mail de recuperação jamais foi enviado a nenhum usuário desde o deploy inicial. Corrigido no código — **⚠️ requer novo deploy em produção pra ter efeito real**, ainda não deployado.
  - Achado sem correção (decisão de produto, não bug óbvio): `change-password` (troca de senha logado) não dispara alerta de segurança por e-mail — só o fluxo de reset via token dispara. O comentário no código sugere o contrário; alinhar antes de decidir se corrige.
  - Achado sem correção (não bloqueia nada): `rateLimitByUser` em `rate-limit.middleware.ts` está implementado mas nenhuma rota o usa — código morto.
  - ~~Adicionar `lint` e testes ao pipeline de CI~~ — **pendência já estava desatualizada**: `test:unit`/`test:integration`/`test:e2e` já rodavam no CI (`.github/workflows/deploy.yml`) desde o bootstrap do repo, só a cobertura de fato é que era mínima (4 testes). `lint` continua não configurado — pendência residual real.
- [x] 2026-07-10 — **Fase 2 do domínio (`bomberquiz.com.br`) migrada e validada em produção** (ver ADR-0028; pegadinhas de execução no Cloudflare documentadas em `arquitetura.md` § Domínios, URLs e cookies):
  1. [x] Nameservers do domínio apontados para o Cloudflare (via hPanel da Hostinger, não Registro.br diretamente); zona ativa.
  2. [x] DNS de `send.bomberquiz.com.br` (Resend) verificado; MX da raiz → Hostinger Business Email, `suporte@bomberquiz.com.br` funcionando; `EMAIL_FROM` atualizado do sandbox `onboarding@resend.dev` para `send.bomberquiz.com.br`.
  3. [x] Frontend: `app.bomberquiz.com.br` como custom domain no Cloudflare Pages (canônico); `www.bomberquiz.com.br` e `www.bomberquiz.com` como redirect 301 — **divergência resolvida com o usuário**: pedido original mencionava `www.` como origem, mas a arquitetura documentada usa `app.`; decisão foi manter `app.` como real e `www.` só como redirect (aditivo à ADR-0028, não a contradiz).
  4. [x] Backend: `api.bomberquiz.com.br` certificado via `flyctl certs add`/`certs show` (Let's Encrypt, `Ready`), registro DNS-only no Cloudflare (proxy desligado, evita conflito com o desafio ACME do Fly).
  5. [x] Redirect `bomberquiz.com` → `bomberquiz.com.br` via segunda zona Cloudflare + Redirect Rule, cobrindo raiz, `www.` **e `app.bomberquiz.com`** (descoberto durante a validação: alguém digitando `app.bomberquiz.com` por engano batia em "site não encontrado" em vez de redirecionar — sem registro DNS pra esse host na zona `.com`; corrigido com A fictício proxied + regra ampliada, preservando path/query string).
  6. [x] `WEB_ORIGIN`/`API_BASE_URL`/`COOKIE_DOMAIN` migrados via `fly secrets set -a bomberquiz-api` para os valores de Fase 2. CORS já é dinâmico a partir de `WEB_ORIGIN` (`api/src/http/app.ts` + `trustedOrigins` em `better-auth.ts`) — nenhum código precisou mudar ali.
  - **🐛 Bug encontrado e corrigido:** três links enviados por e-mail (verificação de cadastro, redefinição de senha, confirmação de troca de e-mail) eram montados em `better-auth.ts`/`request-email-change.usecase.ts` com paths em inglês (`/verify-email`, `/reset-password`, `/profile/email/confirm`) que não existiam no `router.tsx` do frontend (rotas em português: `/verificar-email`, `/redefinir-senha`, `/perfil/email/confirmar`) — 404 do React Router ao clicar. Decisão: como as páginas de destino renderizam conteúdo e o usuário permanece nelas (não é redirect silencioso), a correção foi no backend (paths agora em português), mantendo o `router.tsx` como está.
  - **🐛 Bug encontrado e corrigido:** após migrar os secrets da API, o bundle de produção do `bomberquiz-web` continuou apontando pro `bomberquiz-api.fly.dev` antigo — causa: `VITE_API_BASE_URL` do build de produção está hardcoded em `web/.github/workflows/deploy.yml` (env do step "Build (produção)"), não é lida do painel do Cloudflare Pages (que só recebe o `dist` já pronto via `wrangler-action`, não builda nada). Corrigido trocando o valor pra `https://api.bomberquiz.com.br` + push em `main` (commit `a072e6f`), validado baixando o bundle novo e conferindo a string. Staging (`bomberquiz-api-staging.fly.dev`) não faz parte desta migração e ficou intacto.
  - **Pendência residual:** webhook do Mercado Pago (`arquitetura.md:219`) não foi atualizado para `api.bomberquiz.com.br` — integração ainda não desenvolvida, fica para quando for implementada.
- [ ] **⚠️ Deploy pendente:** a correção do bug de "esqueci minha senha" (endpoint errado, ver acima) está só no repo local — fazer o deploy em produção (`bomberquiz-api`) para os usuários reais serem beneficiados.
- [x] 2026-07-14 — **Telefone/WhatsApp deixa de exigir código de país no cadastro/perfil** (ADR-0029): validação Zod simplificada de `55` + DDD + número (13 dígitos, E.164) para apenas DDD + número (11 dígitos), em backend (`auth.routes.ts`, `me.routes.ts`) e frontend (`features/auth/schemas.ts`, `features/profile/schemas.ts`), com testes e fixtures atualizados e `docs/rf/auth.md` corrigido. DDI fica reservado para o adapter de envio quando o WhatsApp Cloud API virar canal ativo (ADR-0012).
- [x] 2026-07-14 — **Validação estrutural real de telefone (celular/fixo) + máscara no frontend** (ADR-0030), fechando o furo deixado pela ADR-0029 (regra anterior só contava dígitos, sem confirmar celular de verdade nem aceitar fixo — WhatsApp Business roda em linha fixa):
  - **Backend:** value object `Phone` (`domain/user/phone.vo.ts`) + porta `IPhoneValidator` (`domain/user/phone-validator.port.ts`), mesmo padrão hexagonal de `IUserRepository`; adapter `RegexPhoneValidator` (`infra/phone/regex-phone-validator.adapter.ts`) valida DDD contra a lista oficial da Anatel e exige `9` como 3º dígito pra celular (11 dígitos) ou `2-5` pra fixo (10 dígitos), sem biblioteca externa. `register-user.usecase.ts` e `update-profile.usecase.ts` injetam `IPhoneValidator`; `InvalidPhoneError` novo, mapeado pra HTTP 422 `{ code: "invalid_phone" }` em `auth.routes.ts`/`me.routes.ts` (mesmo tratamento de `WeakPasswordError`). Novos testes unit (`regex-phone-validator.test.ts`, `phone.vo.test.ts`) e casos e2e em `me-profile.test.ts`.
  - **Frontend:** `web/src/lib/phone.ts` replica a mesma tabela de DDDs e mensagens (`validateBrazilianPhone`), usado via `.refine()` nos schemas de cadastro e perfil — feedback antes do submit, backend continua fonte de verdade. Componente `PhoneInput` (`web/src/components/phone-input.tsx`) aplica máscara dinâmica `(xx) xxxx-xxxx`/`(xx) xxxxx-xxxx` conforme o usuário digita; valor no React Hook Form e enviado ao backend continua dígitos puros. Novos testes `phone.test.ts` e `phone-input.test.tsx`. Verificado manualmente em navegador (Playwright headless contra o dev server).
  - **Trade-off assumido:** tabela de DDDs duplicada entre `bomberquiz-api` e `bomberquiz-web` (repos independentes, ADR-0008), com comentário cruzado nos dois arquivos — risco baixo, DDDs mudam raramente. `docs/rf/auth.md` e `docs/arquitetura.md` atualizados.
- [x] 2026-07-16 — **Auditoria de tratamento de erros no frontend (`bomberquiz-web`) + entrega real de e-mail (`bomberquiz-api`)**, seguida da implementação completa das recomendações priorizadas (P0–P3):
  - **P0:** `ResendEmailAdapter` (`infra/email/resend.adapter.ts`) passou a checar o campo `error` da resposta do SDK do Resend — nenhum dos 6 métodos checava antes, então falha de envio (domínio inválido, rate limit, bounce) era silenciosamente engolida e o chamador seguia como se o e-mail tivesse sido enviado. Logout fire-and-forget corrigido: `useLogout` (`features/auth/api.ts`) ganhou `onError` central com toast, cobrindo os 3 call sites (`consent-renewal-page.tsx`, `home-page.tsx`, `profile-page.tsx`) de uma vez.
  - **P1:** `RouteErrorBoundary` (novo `errorElement` de topo em `app/router.tsx`) — erro de render não tratado agora cai numa tela de fallback em vez de tela branca. `SessionErrorScreen` + `useSession().isError` (`features/session/guards.tsx`/`use-session.ts`) — falha transitória ao carregar a sessão deixou de ser tratada como "usuário deslogado" (não redireciona mais pra `/login` nesse caso; mostra tela com "Tentar novamente").
  - **P2:** `forgot-password-page.tsx` e `resend-verification-page.tsx` unificados (mesma resposta genérica em sucesso, mas falha real de transporte agora avisa em vez de mentir "e-mail enviado"). `fetchSession` padronizado para usar `ApiError` como o resto do app. Erros de campo do backend (`ApiError.fields`) passaram a ser mapeados para `form.setError()` via novo `lib/api/form-errors.ts`, aplicado em 7 formulários (registro, login, redefinir/trocar senha, trocar e-mail, dados pessoais, desativar/excluir conta).
  - 🐛 **Bug real encontrado durante a verificação em navegador real** (Playwright + Chrome do sistema, não só `tsc`/Vitest): `unwrap()`/`throwIfError()` (`lib/api/errors.ts`) tentavam reler o corpo da resposta via `response.clone().json()`, mas o `openapi-fetch` já consome esse corpo internamente para popular `data`/`error` — o `.clone()` sempre falhava com `"Response body is already used"`, silenciosamente capturado. Na prática, **toda mensagem de erro específica da API (código, campos, mensagem real) vinha caindo no fallback genérico "Erro inesperado. Tente novamente." havia tempos**, incluindo o botão "Reenviar e-mail" do login (dependia de `err.code === "email_not_verified"`, que nunca disparava de verdade). Corrigido consumindo o `error` que o próprio `openapi-fetch` já devolve, em vez de reler `response`.
  - **P3:** retry leve com backoff (até 3×) + log estruturado (e-mail mascarado, sem PII completa) no envio de e-mail; handlers globais `window.onerror`/`unhandledrejection` no frontend (`lib/global-error-handler.ts`), sem serviço externo por ora — decisões e trade-offs em **ADR-0031**, que também registra Sentry como próximo passo natural quando houver usuários reais.
  - **Verificação:** `tsc` limpo nos dois repos, suíte completa passando (106 testes backend — unit/integration/e2e —, 19 frontend), e cada etapa confirmada rodando de verdade em Chrome headless via Playwright (screenshots conferidos, não só testes verdes).
- [x] 2026-07-18 — **Paleta de cores da marca aplicada (`bomberquiz-web`)**, a partir de sugestão de LLM (Vermelho Alerta, Azul Noturno, Laranja Chama, Amarelo Ouro, Branco Gelo) revisada e mapeada nos tokens semânticos do shadcn/ui (`web/src/index.css`, `web/tailwind.config.ts`), com contraste validado por script (WCAG AA, 4.5:1 texto / 3:1 componentes) em vez de estimado. Decisão completa em **ADR-0032**: `--primary` é Azul Noturno, não o vermelho sugerido (evita colidir com a semântica de "ação perigosa" que `--destructive`, já vermelho, carrega). Três tokens novos fora do padrão shadcn, com variante light/dark: `gold`/`flame` reservados para gamificação futura (ranking, pontuação — Módulos 5/6 ainda não existem em código), `ember` (o Vermelho Alerta original) reservado para identidade visual/destaque decorativo, `success` (verde, fora da paleta sugerida) usado em `toast.success()`.
  - Componente reaproveitável `Brand` criado (`web/src/components/brand.tsx`): wordmark "Bomber" (Azul Noturno) + "Quiz" (`ember`), link para `/`, hoje usado em `auth-layout.tsx` no lugar do texto de `title` (que passou a ser só o `aria-label`/nome acessível do heading, preservando navegação por heading para leitores de tela).
  - Auditoria de cor aplicada retroativamente em dois pontos que ainda não usavam tokens semânticos: `web/src/components/ui/sonner.tsx` (`toast.success`/`toast.error` caíam no tema padrão da lib) e `web/src/components/ui/input.tsx` (campo com `aria-invalid` não mudava de cor, só a mensagem abaixo).
- [x] 2026-07-18 — **Módulo 3 (Conteúdo Admin) iniciado — Slice 1: Eixos Temáticos (CONT-RF-001 a 004)**, backend e frontend completos, seguindo a decisão de entrega em fatias verticais incrementais (eixos → matérias → perguntas → fila de revisão; job de dificuldade adiado para quando o Módulo 5/Quiz existir, ver pendência no Backlog).
  - **Backend:** domínio `Axis` (`domain/content/axis.entity.ts`, `.errors.ts`, `.repository.port.ts`) + use cases `list/create/update/archive-axis` (`application/content/`) + `DrizzleAxisRepository`/rotas `admin-axes.routes.ts` (`GET/POST /admin/axes`, `PATCH/POST .../:id[/archive]`). **Primeiro consumidor real** de três peças que já existiam no código mas nunca tinham sido exercitadas: `requireRole("admin")` (RBAC), `PaginationQuerySchema`/`PaginatedResponseSchema`, e transação atômica de escrita (`db.transaction`, nunca usada antes). Isso expôs trabalho de infraestrutura novo, documentado em **ADR-0034**: `ITransactionManager`/`DrizzleTransactionManager` (`infra/persistence/drizzle/transaction-manager.ts`) com detecção de escopo já ativo (via `AsyncLocalStorage` para aninhamento real de use cases, e um sinalizador de módulo dedicado para o `BEGIN`/`ROLLBACK` manual do setup de teste de integração — um spike revelou que `AsyncLocalStorage.enterWith()` não sobrevive à fronteira `beforeEach`→teste no runner do Bun). Tabela `audit_log` criada nesta fatia (primeiro consumidor — nenhum módulo anterior precisava dela), com writer `IAuditLogWriter`/`DrizzleAuditLogRepository` deliberadamente fora do template completo de entity (é só um append-only sem invariante de ciclo de vida).
  - 🐛 **Bug real encontrado e corrigido**: `better-auth.ts` nunca registrava `role` como `additionalField` do Better-Auth — `session.user.role` sempre vinha `undefined`, então `auth.middleware.ts` caía no fallback `"client"` para **qualquer** usuário, inclusive admins reais. `requireRole("admin")` (definido desde antes, nunca usado) estava de fato quebrado para todo mundo até esta fatia virar seu primeiro consumidor real e o teste e2e pegar o 403 inesperado. Corrigido com `user: { additionalFields: { role: { type: "string", defaultValue: "client", input: false } } }` — `input: false` impede que o campo seja setável via endpoints públicos do Better-Auth (promoção a admin continua exclusiva da whitelist/rotas admin dedicadas). ADR-0018 já prescrevia isso desde o spike original; só nunca tinha sido implementado.
  - **Frontend:** guard `RequireAdmin` (`features/session/guards.tsx`, primeiro guard baseado em papel no app — `SessionUser.role` já existia na sessão), feature `features/content/{schemas,axes-api}.ts` (primeira `features/content`), `pages/admin/{axes-page,axis-form-dialog}.tsx`. Componentes shadcn `table`/`dialog`/`badge`/`pagination` adicionados manualmente em `components/ui/` (o CLI `npx shadcn add` criou os arquivos no lugar errado — pasta `@/` literal na raiz do repo em vez de resolver o alias para `src/components/ui/`; descartado, conteúdo copiado manualmente).
  - **🐛 Achado de arquitetura corrigido, registrado em ADR-0033**: a rota óbvia `/admin/eixos` colidia com o proxy do Vite dev server (`vite.config.ts` § `server.proxy`), que intercepta qualquer request começando com `/admin` — incluindo navegação de página inteira, não só chamadas de API — e a redireciona para o backend, causando 404. Como a API já usa (e vai continuar usando) `/admin/*` para dezenas de sub-recursos futuros (`subjects`, `questions`, `users`, `plans`, `courtesies`, `financial`, `coupons`, `ai-generation`), a correção foi mover o **namespace de UI administrativa** para `/painel/*` em vez de tentar manter uma lista de exceções do proxy sincronizada para sempre. Só descoberto porque a verificação usou navegação real de página via Playwright (`page.goto`), não só chamadas de API mockadas — reforça o valor de testar em navegador real além dos testes automatizados.
  - **Verificação:** backend — `bun run test` (unit/integration/e2e) verde contra Postgres real (Docker), incluindo teste de integração que confirma a escrita atômica em `audit_log`; smoke test manual via curl cobrindo 201/409/paginação/arquivar-desarquivar/401/403 e conferência direta do `audit_log` via `psql`. Frontend — `tsc -b`/`vitest run` verdes; fluxo completo (criar → duplicado → editar → desativar/reativar → bloqueio de client comum) verificado de ponta a ponta em Chrome real via Playwright, screenshots conferidos.
  - **Pendência explícita desta fatia** (fechada na Slice 2): `subjects_count` fica hardcoded em `0` (tabela `subjects` ainda não existe) e o arquivamento de eixo não emite o aviso "N matérias ativas" (CONT-RF-004 CA-2) por não haver matérias ainda cadastráveis.
- [x] 2026-07-19 — **Módulo 3 (Conteúdo Admin) — Slice 2: Matérias (CONT-RF-005 a 008)**, backend e frontend completos, reaproveitando toda a infraestrutura da Slice 1 (RBAC, paginação, transação atômica, audit log, guard `RequireAdmin`) sem precisar recriar nada — confirma que o investimento de plumbing da Slice 1 valeu a pena.
  - **Backend:** domínio `Subject` (`domain/content/subject.{entity,errors,repository.port}.ts`) + use cases `list/create/update/archive-subject` (`application/content/`) + `DrizzleSubjectRepository`/rotas `admin-subjects.routes.ts` (`GET/POST /admin/subjects`, `PATCH/POST .../:id[/archive]`). Tabela `subjects` nova (`axis_id` FK sem cascade — eixo nunca é hard-deletado —, índice único funcional `(axis_id, lower(name))` para unicidade **por eixo**, não global). Novo erro cross-aggregate `AxisNotActiveError` (`domain/content/axis.errors.ts`) usado pelos use cases de matéria para validar que `axis_id` aponta pra um eixo existente e `active` (CONT-RF-006 E-1) — mapeado para HTTP 422, não 404 (é erro de validação do corpo, não operação direta sobre o eixo). `UpdateSubjectUseCase` trata o caso de mover de eixo (`axis_id` no PATCH): revalida que o eixo destino está ativo e reroda a checagem de unicidade lá.
  - **Fecha os dois gaps deliberadamente deixados em aberto na Slice 1**: `subjects_count` do eixo passa a refletir a contagem real de matérias `active` (`ISubjectRepository.countActiveByAxisIds`, uma consulta agrupada em vez de N+1, reusada tanto na listagem de eixos quanto no aviso de arquivamento) e `ArchiveAxisUseCase` agora emite o aviso não-bloqueante "Este eixo tem N matérias ativas..." (CONT-RF-004 CA-2) quando arquiva um eixo com matérias ativas — só ao arquivar, não ao reativar.
  - **Padrão novo introduzido**: resposta de "aviso não-bloqueante" (`{ ...entidade, warning: string | null }`) — primeira vez que a API precisa comunicar "sucesso, mas com ressalva" sem usar o envelope de erro. Usado tanto no archive de eixo (aviso real) quanto no de matéria (sempre `null` por ora — `questions_count`/aviso de perguntas publicadas ficam de gap explícito pra Slice 3, mesmo padrão da Slice 1 com `subjects_count`).
  - **Frontend:** componente shadcn `select` (`components/ui/select.tsx`) adicionado manualmente (aprendendo com o mesmo problema do `npx shadcn add` na Slice 1, foi direto pro manual) — usado no seletor de eixo do formulário de matéria (`pages/admin/subject-form-dialog.tsx`), que reaproveita `useAxes()` da Slice 1. `pages/admin/subjects-page.tsx` com filtro adicional por eixo. Rota `/painel/materias`.
  - 🐛 **Achado de teste (não é bug do app)**: `@radix-ui/react-select` chama `Element.prototype.hasPointerCapture`/`scrollIntoView` internamente, APIs que o jsdom não implementa — qualquer interação via Testing Library com um `<Select>` quebra com `"target.hasPointerCapture is not a function"` até essas APIs serem stubadas. Corrigido com polyfills no `tests/setup.ts` (`hasPointerCapture`/`setPointerCapture`/`releasePointerCapture`/`scrollIntoView` como no-ops) — vale para qualquer `<Select>` futuro, não só o desta fatia.
  - **Verificação:** backend — `bun run test` verde (73 testes: unit/integration/e2e, incluindo teste de integração dedicado pro aviso de arquivamento de eixo e pra contagem agrupada); smoke test manual via curl cobrindo o fluxo completo (criar matéria, duplicar nome no mesmo eixo → 409, mesmo nome em eixo diferente → 201, criar em eixo arquivado → 422, mover de eixo com nome já existente no destino → 409, `subjects_count` real). Frontend — `tsc -b`/`vitest run` verdes (29 testes); fluxo completo verificado em Chrome real via Playwright: criar matéria pelo `<Select>` real (não mock), toast de nome duplicado, mover de eixo e conferir a mudança refletida na tabela, e o toast de aviso "N matérias ativas" ao arquivar um eixo com matérias — todos confirmados com screenshot, não só ausência de erro.
- [x] 2026-07-19 — **Correção de arquitetura: `tap_weight` movido de matéria para eixo temático**, achado do usuário ao revisar o Edital TAP/2026 real — o item 7.2 do edital fecha a distribuição das 50 questões **por eixo** (Legislação e Normas=18, Prevenção/Combate a Incêndio=10, Atendimento Pré-Hospitalar=5, Salvamento=9, Ensino/Defesa Civil/SCI=8), não por matéria individual dentro do eixo, ao contrário do que a especificação original assumia desde ADR-0004. Corrigido **antes** de iniciar a Slice 3, sobre trabalho já implementado nas Slices 1 e 2:
  - **Documentação:** `arquitetura.md`, `visao-geral.md`, `requisitos.md` (níveis 1/2 da hierarquia), `decisoes.md` (ADR-0004 atualizada com a fonte da correção), `rf/content-admin.md` (regras gerais + CONT-RF-002/003 ganham `tap_weight`; CONT-RF-005/006/007 perdem) e `rf/quiz.md` (QUIZ-RF-001: simulado TAP agora sorteia por eixo, distribuindo uniformemente entre as matérias ativas de cada eixo — Módulo 5 ainda não implementado, então o ajuste ficou só na especificação).
  - **Backend:** `tap_weight` saiu de `subjects` e entrou em `axes` no schema Drizzle (nova migration `0003_demonic_fabian_cortez.sql`), com o mesmo campo migrando em cascata por `Axis`/`Subject` entities, `content.schemas.ts`, casos de uso de create/update de ambos, repositórios e rotas HTTP. Suíte de testes (unit/integration/e2e) inteiramente realinhada — **73 pass, 0 fail** após a mudança.
  - **Frontend:** campo "Peso no TAP" migrado do formulário/tabela de matéria para o de eixo; tipos OpenAPI regenerados; `axes-page.test.tsx`/`subjects-page.test.tsx` ajustados — **29 pass, 0 fail**, `tsc -b` limpo.
  - **Verificação:** fluxo completo confirmado em Chrome real via Playwright — criar eixo com peso 18, ver a coluna "Peso TAP" refletir na tabela de eixos, confirmar que o formulário de matéria não tem mais esse campo. Dado de teste (usuário/eixo/matéria QA) removido do banco de dev depois.
  - **Nota:** o eixo "Salvamento" pré-existente no banco de dev (criado antes desta correção) ficou com `tap_weight=0` por padrão da migration — precisa ser editado na UI para o valor real do edital vigente (9, conforme Anexo 6).
- [x] 2026-07-19 — **Módulo 3, Slice 3 — Perguntas + upload de imagem (CONT-RF-009 a 013)**, terceira fatia vertical do módulo de Conteúdo.
  - **Decisão de storage revisada antes de começar**: a especificação original (ADR-0012/0016) previa Cloudflare R2 para imagem de pergunta. Ao planejar a fatia, decidimos junto com o usuário trocar para **Cloudinary** — mesmo provedor já decidido para avatar (ADR-0013, ainda não implementado), evitando manter dois provedores de storage de imagem só pela distinção original "estático" vs. "com transformação", que não trazia benefício real. `decisoes.md` (ADR-0012 com nota de revisão), `rf/content-admin.md`, `rf/content-partner.md`, `api.md`, `requisitos.md`, `visao-geral.md` atualizados — R2 permanece só para dump de backup (ADR-0020) e PDFs temporários do Módulo 7 (ADR-0024), inalterados.
  - **Backend:** domínio `Question` (`domain/content/question.{entity,errors,repository.port}.ts`) — invariante de 4 alternativas distintas/não-vazias e `correct_index ∈ 0..3` validado **na construção** (não só no Zod), porque `with()` (usado no PATCH parcial) revalida automaticamente. Toggle de arquivamento com 4 status possíveis (`draft/pending_review/published/archived`, diferente do `active/archived` de eixo/matéria) exigiu um campo novo `status_before_archive` para lembrar o status anterior — sem ele, desarquivar não saberia para onde voltar. Use cases `list/create/update/archive/delete-question` + `upload/delete-question-image` (`application/content/`), `DrizzleQuestionRepository`, rotas `admin-questions.routes.ts`. Tabela `questions` nova (migration `0004_slim_iron_lad.sql`, `alternatives` como `jsonb`, FKs sem cascade para `subjects`/`users`).
  - **Storage de imagem**: `infra/storage/cloudinary.adapter.ts` implementa upload/delete via chamadas HTTP diretas (assinatura SHA-1 manual) à API do Cloudinary — sem SDK adicional, mesmo padrão do `ResendEmailAdapter` (porta definida ao lado do adapter, não em `domain/`). `infra/storage/image-mime.ts` detecta o tipo real da imagem por magic bytes (JPEG/PNG/WEBP), não confiando no `Content-Type` do cliente (CONT-RF-013 CA-1). Novas env vars `CLOUDINARY_CLOUD_NAME`/`CLOUDINARY_API_KEY`/`CLOUDINARY_API_SECRET`.
  - **Upload/delete de imagem são as duas primeiras rotas multipart do projeto** — ficaram fora do contrato `.openapi()` tipado (Hono comum registrado na mesma instância `OpenAPIHono`) porque não há um jeito limpo de expressar upload de arquivo no schema OpenAPI usado aqui; o frontend as chama via `fetch` cru (`features/content/questions-api.ts`), fora do cliente `openapi-fetch`.
  - **Fecha dois gaps deixados em aberto nas Slices 1/2**: `questions_count` da matéria passa a refletir a contagem real de perguntas `published` (`IQuestionRepository.countPublishedBySubjectIds`) e `ArchiveSubjectUseCase` agora emite o aviso "N perguntas dessa matéria deixarão de aparecer em novos quizzes..." (CONT-RF-008 CA-2) ao arquivar matéria com perguntas publicadas.
  - **Endpoint adicionado além do inventário original**: `GET /admin/questions/:id` (detalhe completo) não estava em `api.md`/CONT-RF-009 — só a listagem (com `statement_preview` truncado) estava prevista. Necessário para a tela de edição funcionar (precisa do enunciado/alternativas/gabarito completos). Documentado como CONT-RF-009 CA-5.
  - **Gap identificado, não implementado**: CONT-RF-011 CA-1 não inclui `status` entre os campos editáveis do PATCH — ou seja, uma pergunta criada como rascunho (`?as_draft=true`) hoje **não tem caminho de volta para `published`** via API do admin (só arquivar/desarquivar, que preserva o status original). Não implementamos um endpoint extra para isso por não estar no escopo desta fatia; registrar como pendência caso vire problema real de uso.
  - **Frontend:** `features/content/questions-api.ts` (hooks + `fetchQuestion`/`imageRequest` fora do `openapi-fetch`), `features/content/schemas.ts` (`questionFormSchema`, com `.refine` para rejeitar alternativas duplicadas no cliente também), `pages/admin/questions-page.tsx` (tabela com filtro por eixo/matéria/status/busca, menu de ações via `<DropdownMenu>` — primeiro uso desse componente no projeto, mais apropriado que botões soltos com 3 ações por linha), `pages/admin/question-form-dialog.tsx` (formulário com 4 alternativas + rádio de gabarito, sugestão automática de `reset_stats=true` quando o gabarito muda — CONT-RF-011 CA-5), `pages/admin/image-upload-field.tsx` (componente novo, sem equivalente shadcn). Componentes shadcn `textarea`/`dropdown-menu` adicionados. Rota `/painel/perguntas`.
  - 🐛 **Bug real encontrado via teste manual em navegador (não pego pelos testes automatizados)**: `z.coerce.boolean()` no query param `?as_draft=false` resultava em `true` — `Boolean("false")` é `true` em JS, porque qualquer string não-vazia é truthy. Toda pergunta criada como "publicar direto" nascia como rascunho. Corrigido com um schema explícito (`z.enum(["true","false"]).transform(v => v === "true")`) reaplicado também em `has_image` (mesma armadilha, ainda não observada em produção porque o filtro não era exercitado). Teste de regressão adicionado em `admin-questions.test.ts`. Confirmado que era o único uso de `z.coerce.boolean()` no backend.
  - **Verificação:** backend — `bun run test` verde (89 e2e + 46 integration + 71 unit = 206 testes); stub de rede para a API do Cloudinary (`tests/helpers/mock-cloudinary.ts`, mesmo padrão do `mock-resend.ts`) usado só em e2e, já que nenhum teste de integração hoje bate em provedor externo (mesmo padrão dos fluxos de e-mail). Frontend — `tsc -b`/`vitest run` verdes (33 testes). Fluxo completo confirmado em Chrome real via Playwright: criar pergunta publicada direto, editar, arquivar/desarquivar (com o bug do `as_draft` pego exatamente nesse passo), excluir definitivamente, e tentativa de upload de imagem tratada com erro visível ao usuário (toast), não crash.
  - **Pendência descoberta durante a verificação manual**: as credenciais Cloudinary em `.env` (dev) têm `cloud_name` inválido (`Invalid cloud_name bomberquizdev`, confirmado via chamada direta à API) — upload real de imagem não funciona até o usuário corrigir o valor no dashboard do Cloudinary. Resto do fluxo (CRUD de pergunta, arquivamento, exclusão) não depende disso e foi verificado normalmente.
- [x] 2026-07-20 — **Módulo 3, Slice 4 — Fila de revisão de parceiro (CONT-RF-014 a 016)**, quarta e última fatia desta rodada do Módulo 3 (o job de dificuldade, CONT-RF-017, segue adiado separadamente — ver Backlog).
  - **Escopo desta fatia é só o lado admin**: listar pendências (FIFO), aprovar e rejeitar. O lado parceiro (PART-RF-007 histórico, PART-RF-008 dashboard/badge) é Módulo 4, que ainda não existe em código — decisão tomada com o usuário antes de começar (ver "Gaps explícitos" abaixo).
  - **Backend:** `Question` ganhou `reviewedBy`/`reviewedAt`/`rejectionReason` (migration `0005_heavy_donald_blake.sql`, 3 colunas nullable + FK em `reviewed_by`) e dois métodos de transição, `approve()`/`reject()` (mesmo padrão de `archive()`/`unarchive()` — transição pura, a precondição `status === pending_review` é checada no use case via novo erro `QuestionNotPendingReviewError`, 409). Novos use cases `list-pending-questions`, `approve-question`, `reject-question` (`application/content/`) e rotas `GET /admin/questions/pending` (FIFO + `partner_pending_count` via novo `IQuestionRepository.countPendingByAuthorIds`), `POST /admin/questions/:id/approve`, `POST /admin/questions/:id/reject` (motivo 10-500, 422 se fora da faixa).
  - **Notificação ao parceiro por e-mail** (CONT-RF-015 CA-4 / CONT-RF-016 CA-3): dois métodos novos em `IEmailPort`/`ResendEmailAdapter` (`sendQuestionApproved`/`sendQuestionRejected`), disparados como best-effort (try/catch fora da transação) a partir dos use cases — falha de envio não derruba a aprovação/rejeição já persistida, mesmo espírito do delete best-effort de imagem no Cloudinary.
  - **Gaps explícitos (decisão tomada com o usuário antes de implementar, não esquecimento)**: (1) contador `unread_review_events`/badge do parceiro — **não implementado nesta fatia**, zero schema criado, porque não há nenhum consumidor real ainda (Módulo 4 não existe); fica para quando o Módulo 4 desenhar isso com um endpoint de verdade (provável `last_seen_review_events_at` por par usuário/pergunta, ver PART-RF-007 CA-4). (2) O e-mail de aprovação/rejeição **não inclui link direto para a pergunta** (previsto em CONT-RF-015 CA-4/CONT-RF-016 CA-3) — não existe tela de parceiro para linkar ainda; e-mail traz só o resumo do enunciado (e o motivo, na rejeição). Revisitar os dois gaps junto do Módulo 4.
  - **Dados de teste**: como não existe endpoint de submissão de parceiro (Módulo 4), testes e verificação manual semeiam perguntas `pending_review` diretamente via repositório/SQL, bypassando o HTTP — mesmo padrão já usado para outros fluxos não implementados. Novo helper `registerPartnerUser()` em `tests/helpers/admin.ts` (mesmo molde de `registerAdminUser()`).
  - **Frontend:** `usePendingQuestions`/`useApproveQuestion`/`useRejectQuestion` (`features/content/questions-api.ts`), `pages/admin/review-queue-page.tsx` (tabela com colunas extras `submitted_at`/`partner_pending_count`, botão direto "Aprovar" e diálogo "Rejeitar" com motivo obrigatório), `pages/admin/reject-question-dialog.tsx` (segundo diálogo do projeto com campo de texto obrigatório, mesma composição Dialog+Form+Textarea de `question-form-dialog.tsx`). Rota `/painel/revisao`.
  - 🐛 **Bug pré-existente encontrado e corrigido durante a regeneração dos tipos OpenAPI** (não introduzido nesta fatia): `schema.d.ts` do frontend estava desatualizado desde a correção do bug `z.coerce.boolean()` na Slice 3 — o tipo gerado para `as_draft` ainda era `boolean` quando o contrato real já exigia `"true"|"false"` (string literal) desde aquela correção. Não era um bug funcional em produção (o `openapi-fetch`/`URLSearchParams` sempre serializou o boolean como `"true"`/`"false"` na querystring de qualquer forma), mas quebrava `tsc -b` assim que os tipos foram regenerados corretamente. Corrigido em `useCreateQuestion` (`features/content/questions-api.ts`) convertendo explicitamente antes de enviar; teste correspondente em `questions-page.test.tsx` ajustado.
  - **Verificação:** backend — `bun run test` verde, 220 testes no total (73 unit, 53 integration, 94 e2e — +14 sobre a Slice 3), incluindo teste de integração dedicado à ordem FIFO (`order: "asc"`) e ao `countPendingByAuthorIds`, e e2e cobrindo aprovar/rejeitar/409/422/404/403 com e-mail capturado via `stubResendApi()`. Frontend — `tsc -b`/`vitest run` verdes (37 testes, +4 novos). Smoke test manual via curl contra a API de dev confirmando FIFO, `partner_pending_count`, aprovar, rejeitar (422 curto, sucesso válido), 409 em dupla aprovação, fila esvaziando. Fluxo completo confirmado em Chrome real via Playwright (dados de teste semeados via SQL e removidos depois): fila carregando com autor/contagem de pendências, validação client-side do motivo (<10 caracteres), rejeição com toast de sucesso e remoção da fila, aprovação com toast de sucesso e remoção da fila — screenshots conferidos, não só ausência de erro.
  - **Com esta fatia, a rodada de Módulo 3 planejada (Eixos → Matérias → Perguntas → Fila de revisão) está completa.** Só resta CONT-RF-017 (job de dificuldade), adiado para o Módulo 5.
- [x] 2026-07-20 — **Header da área do painel (`/painel/*`), mobile-first** — primeiro elemento de navegação/chrome compartilhado do projeto (as 4 páginas administrativas não tinham nenhum jeito de navegar entre si a não ser digitando a URL).
  - **Componente novo**: `web/src/components/ui/sheet.tsx` (shadcn `sheet`, hand-authored sobre `@radix-ui/react-dialog` já instalado — mesmo padrão manual dos demais componentes shadcn deste projeto, sem dependência nova). `web/src/components/panel-layout.tsx` — abaixo de `md` (768px) vira drawer lateral (`Sheet`) com os 4 links + dados do usuário + "Sair"; a partir de `md` os links ficam inline no header (primeiro uso de `NavLink` no projeto, para estilo de link ativo) e o usuário ganha um `DropdownMenu` (componente já existente, reaproveitado) com "Meu perfil"/"Início"/"Sair". Logout reaproveita `useLogout()` sem mudanças.
  - **Router**: `web/src/app/router.tsx` — `PanelLayout` inserido como layout route entre `RequireAdmin` e as 4 rotas `/painel/*` existentes; as páginas em si não mudaram.
  - **Verificação**: `tsc -b`/`vitest run` verdes (44 testes, +4 novos em `panel-layout.test.tsx`, primeiro teste de layout/nav do projeto). Fluxo completo confirmado em Chrome real via Playwright em dois viewports (375px e 1280px): mobile mostra só hambúrguer (nav inline de fato oculta por CSS real, não só ausente do DOM), abre o drawer, navega e fecha sozinho; desktop mostra nav inline sem hambúrguer, menu do usuário abre e "Sair" desloga e redireciona — screenshots conferidos nos dois breakpoints.

- [x] 2026-07-22 — **Módulo 5 (Quiz cliente) iniciado — Slice 1: loop principal (QUIZ-RF-001 a 003)**, primeira fatia de um ciclo de desenvolvimento planejado em 6 fatias (backend: loop principal → scheduler/expiração → resultado/histórico → desempenho/timeline/reset; depois frontend: fluxo de quiz → histórico/desempenho). **Decisão de escopo tomada com o usuário antes de começar**: QUIZ-RF-009 (bloqueio por falta de assinatura ativa) depende do Módulo 6, que não existe — o Quiz é construído **completo mas liberado para qualquer usuário autenticado** (mesmo `requireAuth` das demais rotas de cliente), sem `access_status`/trial/HTTP 402 nesta rodada; o gate fica para quando o Módulo 6 for implementado, com a estrutura de rotas já deixando a emenda pronta (só `POST /quizzes` e `POST /quizzes/:id/answers` — as duas que a spec exige bloquear — ganhariam um segundo middleware na mesma cadeia `.use()`).
  - **Backend:** `domain/quiz/` — `QuizSession`/`QuizSessionQuestion` (entidades com transições puras `finish()`/`expire()`/`abandon()`/`recordAnswer()`/`answer()`/`markUnansweredAsWrong()`), `draw.ts` (funções puras de sorteio/embaralhamento com `crypto.randomInt`, sem porta injetável — mesmo estilo pragmático já usado para `randomUUID()` no resto do código), `quiz.errors.ts`. Três tabelas novas (migration `0006_strong_moira_mactaggert.sql`): `quiz_sessions`, `quiz_session_questions` (snapshot da pergunta em colunas denormalizadas — não um blob JSON único — espelhando o shape de `questions`, incluindo `alternatives_snapshot` já embaralhado e `correct_index_snapshot` relativo a essa ordem embaralhada; `subject_id`/`subject_name_snapshot` denormalizados para sobreviver a uma pergunta ser reatribuída de matéria depois), `user_subject_stats` (chave primária composta `user_id, subject_id`, upsert incremental via `ON CONFLICT DO UPDATE`). Use cases `start-quiz`/`submit-answer`/`finish-quiz` (`application/quiz/`) + `assemble-quiz-result.ts` (montagem do payload de resultado compartilhada entre `finish-quiz` e o futuro `get-quiz-result` da Slice 3, já que QUIZ-RF-003 CA-3 exige o mesmo payload de QUIZ-RF-005). `IQuestionRepository`/`IAxisRepository` ganharam métodos de pool de sorteio (`findPublishedByAxisId`/`findPublishedBySubjectId`/`countPublishedByAxisIds`/`findAllActiveWithPositiveTapWeight`). Rotas `http/routes/quizzes.routes.ts` (`POST /quizzes`, `POST /quizzes/:id/answers`, `POST /quizzes/:id/finish`).
  - **Regra de negócio não-óbvia implementada com cuidado**: assimetria intencional de estatísticas entre abandono (QUIZ-RF-001 CA-6 — auto-abandono ao iniciar novo quiz com um `in_progress` existente) e finalização/expiração (QUIZ-RF-003/004 — questões não respondidas contam como erro). Os dois caminhos passam por um helper único de "marcar não respondidas como erradas" (`markUnansweredQuestionsAsWrong` no repositório, `UPDATE` de uma instrução só via `WHERE answered_at IS NULL`), mas o fluxo de abandono deliberadamente **não o chama** — a diferença fica explícita no código, não escondida numa flag.
  - **Verificação:** `bun run typecheck` limpo nos dois repos. Backend — 89 testes unit (100% novos de quiz passando: `draw.test.ts`, `quiz-session.entity.test.ts`), suíte e2e completa (100 testes, 6 novos em `quizzes.test.ts`) verde contra Postgres real — fluxo completo iniciar→responder todas (after_each, revela gabarito por resposta)→auto-finalização, modo at_end (não revela) + finalizar manual com questões em branco (marca erro, resultado sempre revela gabarito), rejeição de resposta duplicada, matéria com perguntas insuficientes (409), simulado TAP somando `tap_weight` de múltiplos eixos. Testes de integração (`tests/integration/{start-quiz,submit-answer,finish-quiz}.usecase.test.ts`) escritos e com lógica confirmada correta individualmente, mas **hits a limitação de ambiente já documentada** (ver abaixo) ao rodar a suíte completa — por isso a suíte e2e (que não usa o hack de transação manual) foi o caminho de verificação primário para esta fatia.
  - **Achado sobre a limitação de ambiente conhecida (Windows/Bun, ver seção `## Test` do skill `run-bomberquiz-api`)**: confirmado que o hang de `beforeEach`/`afterEach` não é exclusivo dos 4 arquivos já catalogados (`create-axis`/`create-subject`/`create-question`/`review-question`) — reproduz também nos novos testes de quiz, e **em ambos os casos correlaciona com testes que esperam que o use case lance um erro** (`.rejects.toBeInstanceOf(...)`), não com o arquivo inteiro. Confirmado isolando um teste de cada vez via `bun test -t "<nome>"`: testes de caminho feliz passam limpos: mesmo teste de erro hang mesmo sozinho, em processo novo, sem contenção de outro processo (verificado matando um processo `bun` travado e checando `pg_stat_activity` — nenhuma transação presa). Ainda não investigada a causa raiz; registrada como refinamento da pendência já conhecida, não uma regressão nova desta fatia.
  - **Próximos passos deste ciclo**: Slice 2 (scheduler in-process via `croner` + expiração automática/auto-abandono por job, QUIZ-RF-004 — primeiro conteúdo real de `infra/scheduler/`, hoje só `.gitkeep`), Slice 3 (resultado/histórico, QUIZ-RF-005/006), Slice 4 (desempenho/timeline/reset, QUIZ-RF-007/008/010), depois as duas fatias de frontend.

- [x] 2026-07-23 — **Módulo 5, Slice 2 — Scheduler de expiração/auto-abandono de quiz (QUIZ-RF-004 CA-4)**, primeiro conteúdo real de `infra/scheduler/` (antes só `.gitkeep`) e primeira dependência de runtime nova desde o bootstrap do backend (`croner`, ADR-0035).
  - **Backend:** `QuizSession.isInactiveSince(now)` (domínio, `quiz-session.entity.ts`) — janela fixa de 24h (CA-6), mesmo estilo de `isExpiredByTimer`. `IQuizSessionRepository.findAllInProgress()` (porta + `DrizzleQuizSessionRepository`) — uma única query por `status` (reaproveita `quiz_sessions_status_idx`, já pensado para isso desde a Slice 1); a decisão entre expirar/abandonar fica no use case, evitando duplicar em SQL a matemática de tempo que já vive na entidade. Novo caso de uso `ExpireAndAbandonQuizzesUseCase` (`application/quiz/expire-and-abandon-quizzes.usecase.ts`): busca todos os `in_progress`, aplica **precedência do cronômetro sobre inatividade** (se ambos vencem ao mesmo tempo, expira — informação mais específica), transação por sessão via `txManager.run()` (não um lote único — falha isolada não desfaz sessões já corretas nem trava o próximo tick), `try/catch` por sessão com contagem de falhas sem interromper o job. `infra/scheduler/scheduler.ts` (novo): `registerJob`/`startScheduler` sobre `croner`, fuso `America/Sao_Paulo`, `protect: true`, erro por job capturado e logado sem derrubar o processo nem travar os demais (ADR-0035). Wiring em `src/index.ts` após `Bun.serve`. Nova env `QUIZ_EXPIRY_JOB_CRON` (default `*/5 * * * *`, `env.ts`/`.env.example`/`.env.test`).
  - 🐛 **Bug real encontrado durante o planejamento e corrigido nesta fatia**: a expiração "on-demand" em `submit-answer.usecase.ts` (quando uma submissão tardia expira o quiz no meio de `POST /quizzes/:id/answers`) marcava questões em branco como erradas mas **nunca atualizava `user_subject_stats`** — diferente de `finish-quiz.usecase.ts`, que sempre atualizou corretamente. A spec (QUIZ-RF-004 CA-3) exige que a expiração siga a mesma regra de QUIZ-RF-003 CA-2, que inclui as stats. Corrigido extraindo um helper compartilhado, `markUnansweredAsErrorsAndUpdateStats` (`application/quiz/mark-unanswered-as-errors.ts`), usado agora pelos três lugares que "marcam não respondidas como erro" (`finish-quiz`, `submit-answer`, e o novo job) — elimina a duplicação que causou a divergência, em vez de só replicar o comportamento certo no job novo. Decisão de escopo (corrigir agora vs. só no job novo) tomada com o usuário antes de implementar.
  - **Achado sobre o ambiente de testes**: a suíte completa (`test:unit` 92, `test:integration` 77, `test:e2e` 100 — todas verdes) rodou sem o hang de `beforeEach`/`afterEach` documentado na entrada da Slice 1 — essa limitação é específica de Windows/Bun (confirmado lá) e não reproduz neste ambiente Linux; não precisou isolar nenhum arquivo com timeout.
  - **Verificação:** `bun run typecheck` limpo; `test:unit` (92 pass, +3 novos de `isInactiveSince`), `test:integration` completo (77 pass, incluindo os 6 casos novos de `expire-and-abandon-quizzes.usecase.test.ts`: expira por timer + atualiza stats, abandona por inatividade sem penalizar, precedência timer-sobre-inatividade, não mexe em quiz ativo, múltiplas sessões candidatas, idempotência com sessão já finalizada) e regressão em `submit-answer.usecase.test.ts` provando a correção do bug de stats; `test:e2e` completo (100 pass). `bun run dev` confirmado no log de boot: `[Scheduler] job "quiz-expire-and-abandon" registrado (*/5 * * * *, America/Sao_Paulo)`. Smoke manual direto contra o Postgres de dev (fora de transação de teste, dados removidos depois): sessão com cronômetro vencido virou `expired`, questões em branco marcadas como erro, `user_subject_stats` refletindo `total_answers=5, correct_answers=0` — confirma o comportamento persistido de verdade, não só dentro do rollback dos testes de integração.

- [x] 2026-07-23 — **Módulo 5, Slice 3 — Resultado + histórico de quiz (QUIZ-RF-005/006)**, terceira fatia do ciclo — essencialmente wiring sobre o que já existia das Slices 1/2 (`assembleQuizResult`, `QuizResultSchema`, `toResultResponse`), sem tabela nem regra de domínio nova além de um erro.
  - **Backend:** `GET /quizzes/:id/result` (QUIZ-RF-005) — novo `GetQuizResultUseCase` (`application/quiz/get-quiz-result.usecase.ts`), puramente leitura: busca a sessão, valida posse (`QuizNotFoundError` senão) e que já saiu de `in_progress` (novo `QuizStillInProgressError` — 409 `quiz_still_in_progress` — senão), e reaproveita `assembleQuizResult` tal qual `finish-quiz` já fazia. `GET /me/quizzes` (QUIZ-RF-006) — novo `ListQuizSessionsUseCase` + `IQuizSessionRepository.listByUserId()`: filtros `mode`, `status` (subconjunto de `finished/expired/abandoned`, default exclui `in_progress` — CA-2) e intervalo de datas (`started_from`/`started_to`), paginado, `started_at DESC`. `scope_id` não tem FK fixa (aponta para `subjects` ou `axes` conforme `mode`, ver Slice 1) — o nome é resolvido com dois `leftJoin` condicionados pelo próprio `mode` numa única query (`COALESCE` em memória), sem N+1 nem segunda rodada de lookups. Rota registrada em `quizzes.routes.ts` (fora do prefixo `/quizzes/*` — ganhou seu próprio `.use("/me/quizzes", requireAuth)` no mesmo arquivo, evitando reinstanciar repositórios em `me.routes.ts`).
  - **Verificação:** `bun run typecheck` limpo; `test:unit` (92 pass, inalterado — nenhuma regra de domínio nova além do erro); `test:integration` (88 pass, +11 novos: 5 em `get-quiz-result.usecase.test.ts` — resultado após finalizar, quiz de outro usuário, quiz inexistente, ainda em andamento, quiz abandonado também consultável — e 6 em `list-quiz-sessions.usecase.test.ts` — só sessões do próprio usuário mais recente primeiro, default exclui `in_progress`, filtro por `mode` com `scope_name` de eixo, `scope_name` nulo em `tap_simulation`, filtro de intervalo de datas, paginação); `test:e2e` completo (103 pass, +3 novos em `quizzes.test.ts`: 200/404/409 do resultado, histórico paginado com filtro de status excluindo `in_progress` por padrão, 401 sem cookie).
  - **Achado durante a escrita dos testes e2e** (não um bug de produção — erro no próprio teste, registrado porque o sintoma inicial confundia): um teste novo semeou 5 perguntas mas pediu `size: 10` ao iniciar o quiz, causando `409 subject_insufficient_questions` silencioso no `POST /quizzes` — a resposta de erro (sem `quiz_id`) fez o teste seguinte de `GET .../undefined/result` retornar 404 por acidente, mascarando o que parecia (e não era) um bug de roteamento. Corrigido igualando a seed ao `size` pedido, como o resto do arquivo já fazia.

- [x] 2026-07-23 — **Módulo 5, Slice 4 — Painel de desempenho + reset + timeline (QUIZ-RF-007/008/010)**, quarta e última fatia de backend do ciclo antes das duas fatias de frontend. QUIZ-RF-009 (gate de assinatura) permanece adiado para o Módulo 6, como já decidido na Slice 1.
  - **Backend:** `GET /me/performance` (RF-007) — novo `GetPerformanceUseCase`, sem porta nova em `IUserSubjectStatsRepository` (reaproveita `findAllForUser`, já existente); resolve nomes de matéria/eixo com o mesmo padrão `Promise.all(ids.map(findById))` já usado em `start-quiz.usecase.ts`. `status_badge` (`unrated`/`fraco`/`medio`/`forte`, fronteiras 10 respostas / 50% / 75%) extraído como função pura exportada (`statusBadge`), testável isoladamente. `by_axis` somado em runtime a partir das matérias; `weakest_subjects`/`strongest_subjects` = top 3 por accuracy entre matérias com ≥10 respostas. `POST /me/performance/reset` (RF-008) — novo `ResetPerformanceStatsUseCase`, reaproveitando **integralmente** a infraestrutura já implementada na Slice 1 antecipando este RF: `user_subject_stats.stats_reset_at` (coluna) e `IUserSubjectStatsRepository.resetAllForUser()` (método) já existiam prontos, sem precisar de nenhuma mudança de schema ou repositório nesta fatia. Reautenticação por senha replica o truque já usado em `DeactivateAccountUseCase` (`auth.api.changePassword` com a mesma senha, só para validar); grava `audit_log` (`action=reset_own_stats`) dentro da mesma transação do reset. Checkbox de confirmação (CA-1/E-2) resolvido só no schema Zod (`confirm: z.literal(true)`), sem lógica extra no use case. `GET /me/performance/timeline` (RF-010) — novo `GetPerformanceTimelineUseCase` + dois métodos novos em `IQuizSessionRepository`/Drizzle (`getMonthlyAnswerAggregates`, `getMonthlyFinishedAggregates`), agregando por mês civil via `to_char(... AT TIME ZONE 'America/Sao_Paulo', 'YYYY-MM')` sobre o índice `quiz_session_questions_answered_at_idx` (já existia desde a Slice 1, com o comentário explícito antecipando esta fatia). Gerador puro de meses (`buildRecentMonthKeys`) também exportado e testado isoladamente. As três rotas foram para `quizzes.routes.ts` (mesmo motivo de `/me/quizzes` na Slice 3 — fora do prefixo `/quizzes/*`, ganharam `.use("/me/performance", requireAuth)` + `.use("/me/performance/*", requireAuth)` próprios).
  - **Ajuste ao plano durante a implementação**: o plano original previa um teste de integração dedicado para `ResetPerformanceStatsUseCase`, mas a exploração do código mostrou que os dois fluxos análogos já existentes (`DeactivateAccountUseCase`/`DeleteAccountUseCase`) também não têm teste de integração — só e2e — porque a reautenticação depende de uma credencial real do Better-Auth, que `buildTestUser()` (inserção direta via `IUserRepository`, usada nos demais testes de integração deste módulo) não cria. Seguida a mesma convenção: cobertura de `ResetPerformanceStatsUseCase` movida inteiramente para `tests/e2e/performance.test.ts` (via `registerVerifiedUser`, que registra de verdade pelo HTTP).
  - **Verificação:** `bun run typecheck` limpo; `test:unit` (99 pass, +7 novos: 4 de `statusBadge` nas fronteiras, 3 de `buildRecentMonthKeys`); `test:integration` (99 pass, +11 novos: 5 em `get-performance.usecase.test.ts` — overall/by_axis, as 4 fronteiras de `status_badge`, piso de 10 respostas em weakest/strongest, `quizzes_finished`, `stats_reset_at` null — e 6 em `get-performance-timeline.usecase.test.ts` — mês atual, mês vazio com `accuracy: null`, `months` customizado, agregação de mês anterior manipulado diretamente, truncamento após reset, assimetria CA-7 em quiz abandonado); `test:e2e` completo (112 pass, +9 novos em `performance.test.ts`: painel após responder um quiz real, reset com senha certa/errada/sem confirmação, timeline default e customizada, `months` fora do intervalo 1-24, 401 sem cookie nos três endpoints).
  - **Achado durante a escrita dos testes e2e** (de novo erro no próprio teste, não em produção — mesma classe do achado da Slice 3): dois bugs de setup corrigidos antes de os testes passarem — (1) submeter `submitted_index: 0` não garante acerto, já que as alternativas são embaralhadas por sessão (QUIZ-RF-001 CA-3), então o teste do painel foi ajustado para não assumir 100% de acerto e validar a consistência dos números em vez de um placar fixo; (2) `size: 5` não é um valor válido do enum de tamanho de quiz na API (`10|20|30|50`), causando 422 silencioso — ajustado para `size: 10` com seed de 10 perguntas.

- [x] 2026-07-24 — **Módulo 5, Slice 5 — Frontend do fluxo de quiz (QUIZ-RF-001 a 005)**, primeira das duas fatias de frontend do ciclo, cobrindo iniciar → responder → resultado. QUIZ-RF-009 (gate de assinatura) segue adiado para o Módulo 6.
  - **Gap de backend descoberto e fechado antes do frontend**: não existia endpoint que um cliente comum (role=client) pudesse usar para listar eixos/matérias ativos — só `/admin/axes`/`/admin/subjects`, restritos a `role=admin`, inutilizáveis pela tela de iniciar quiz (modos `free_subject`/`free_axis` exigem escolher matéria/eixo). Decisão tomada com o usuário: novo arquivo `catalog.routes.ts` com `GET /axes`/`GET /subjects` (`requireAuth`, qualquer papel), reaproveitando **integralmente** `ListAxesUseCase`/`ListSubjectsUseCase` (já genéricos, do Módulo 3, chamados com `status: "active"`) — nenhum use case novo, schemas de resposta propositalmente enxutos (`ClientAxisSchema`/`ClientSubjectSchema`, sem os campos administrativos). Cobertura só em e2e (`catalog.test.ts`, 4 testes), já que a lógica de listagem em si já tinha cobertura no Módulo 3.
  - **Frontend**: três rotas novas (`/quiz/iniciar`, `/quiz/:quizId`, `/quiz/:quizId/resultado`) com estado inteiramente no servidor — sem wizard local. `AnswerQuizPage` recebe as perguntas via `location.state` (retorno de `POST /quizzes`, sem gabarito), nunca refetch: não existe `GET /quizzes/:id` para "retomar" um quiz em andamento, e a spec já define o quiz como efêmero (sem pause/resume) — F5 no meio perde a sessão de propósito, com tela própria orientando reiniciar. `ResultQuizPage` já busca sempre via `GET .../result` (`location.state` só como `initialData`, nunca fonte única), preparando o terreno para links de histórico na Slice 6. Cronômetro (`useQuizCountdown`) recalcula o tempo restante a cada tick a partir de `started_at + time_limit_seconds` (não decrementa contador local); ao chegar a zero, desabilita input e chama `POST .../finish` automaticamente — com uma ressalva documentada no código: esse caminho específico produz `status: finished` em vez de `expired` (`FinishQuizUseCase` não verifica o relógio), diferente do caminho principal (409 `quiz_expired` numa submissão real após o prazo do servidor), que sai correto; puramente cosmético, já que a pontuação é idêntica nos dois casos. Quatro componentes shadcn novos (`switch`, `slider`, `radio-group`, `progress`).
  - **Verificação:** `bun run typecheck` limpo nos dois repos; backend `test:unit` (99 pass, inalterado) e `test:e2e` completo (116 pass, +4 novos de `catalog.test.ts`); frontend `test` completo (53 pass em 13 arquivos, +13 novos entre `start-quiz-page`/`answer-quiz-page`/`result-quiz-page`). Fluxo completo verificado num Chrome real via Playwright ad-hoc (não só testes verdes): login → iniciar quiz livre por matéria → responder 10 questões em `after_each` (banner de acerto/erro + gabarito) → tela de resultado (stats, breakdown por matéria, revisão completa) numa primeira rodada; `at_end` (sem revelar gabarito) + encerramento manual com pendências (diálogo com a contagem exata de questões em branco) numa segunda — screenshots conferidos.
  - **Achados corrigidos durante a implementação** (nenhum em produção — infraestrutura de dev/teste):
    - `bunx shadcn@latest add` escreveu os 4 componentes num diretório literal `web/@/components/ui/` em vez de resolver o alias `@/` para `src/` (apesar de `components.json`/`tsconfig.app.json` corretos) — movidos manualmente, sem re-rodar o CLI.
    - `web/vite.config.ts` tinha o proxy de dev listando prefixos de rota explícitos (`/admin`, `/quizzes` etc.) sem incluir os dois novos (`/axes`, `/subjects`) — sem o ajuste, o dev server serviria o fallback SPA em vez de repassar pro backend. Corrigido junto.
    - `web/tests/setup.ts` não stubava `ResizeObserver` (só Pointer Events/scroll, usados pelo `Select` já existente) — `@radix-ui/react-{radio-group,switch,slider}`, usados pela primeira vez nesta fatia, quebravam todo teste que os montasse. Stub adicionado ao setup global.
    - Bug real de UI pego pelos próprios testes (React avisando "changing from uncontrolled to controlled"): os `RadioGroup` de alternativas/tamanho do quiz começavam com `value={undefined}` antes de qualquer seleção — corrigido com fallback `?? ""`, mesmo padrão que o Módulo 3 já usa em `Select` (`subjectId: question?.subjectId ?? defaultSubjectId ?? ""` em `question-form-dialog.tsx`), só que eu tinha deixado de aplicar aqui.
    - Verificação manual em navegador precisou de 10 perguntas publicadas na matéria semeada, não 5 — `StartQuizUseCase` exige `pool.length >= size`, não só o piso fixo de 5 (mesma pegadinha já registrada nas Slices 3/4 sobre casar a seed com o `size` pedido, desta vez fora dos testes automatizados, na seed manual mesmo).

- [x] 2026-07-24 — **Módulo 5, Slice 6 — Frontend de histórico + desempenho + reset (QUIZ-RF-006/007/008/010)**, última fatia do módulo. Sem nenhum gap de backend — os 4 endpoints já existiam desde as Slices 3-4 e já estavam no cliente OpenAPI tipado (regenerado na Slice 5). Primeira dependência de gráfico do projeto: **`recharts`** (ADR-0036) instalado.
  - **Reaproveitamento pesado de padrões já validados**: `ResetStatsSection` é praticamente uma cópia de `DeleteAccountDialog` (`pages/profile/danger-zone-section.tsx` — `AlertDialog` + senha + checkbox `confirm: z.literal(true)`, mesmo formato de `deleteAccountSchema`); `HistoryPage` segue o padrão de filtro com `<select>` cru + `Pagination` já de `questions-page.tsx`; `PerformancePage` reaproveita o padrão de página-composta-por-seções de `profile-page.tsx`; e o link "Ver resultado" do histórico aponta direto pra `ResultQuizPage` da Slice 5 (`/quiz/:id/resultado`) — **nenhuma tela de resultado nova**, já que aquela página busca por `GET .../result` de forma independente, sem depender de `location.state`.
  - **Resolvida a pendência do ADR-0036** sobre paleta categórica: não foi necessária. O painel de desempenho colore barras por `status_badge` (ordinal — unrated/fraco/medio/forte), não por identidade de matéria, então os tokens semânticos já existentes (`--muted-foreground`, `--destructive`, `--gold`, `--success`, ADR-0032) bastam; a timeline é série única (accuracy ao longo do tempo), sem necessidade de paleta nenhuma.
  - **Verificação:** `bun run typecheck` limpo; `bun run test` completo (61 pass em 16 arquivos, +8 novos entre `history-page`/`performance-page`/`reset-stats-section`). Fluxo completo verificado num Chrome real via Playwright ad-hoc, reaproveitando o cliente/histórico já semeado na verificação manual da Slice 5: histórico com 4 quizzes reais (filtro por status, link "Ver resultado" abrindo o resultado correto), painel de desempenho com gráfico de barras colorido corretamente por `status_badge` (tooltip funcionando), listas de pontos fracos/fortes, timeline com troca de período (3/6/12/24 meses), reset de estatísticas nos dois caminhos — senha errada (toast "Senha incorreta.", diálogo permanece aberto) e sucesso (toast "Estatísticas zeradas.", painel e timeline refletem o zerado **sem reload**, banner "Você zerou suas estatísticas em X" aparece na timeline) — screenshots conferidos.
  - **Achado, não um bug**: depois do reset, `user_subject_stats` mantém as linhas por matéria (zeradas), não as apaga — então "matérias estudadas" e a barra da matéria continuam aparecendo no gráfico (só que em 0%), em vez de a lista ficar vazia. Comportamento correto por design (QUIZ-RF-008 CA-2: "zera total_answers e correct_answers", não deleta a linha) — só documentando porque a suposição inicial do script de verificação manual era a oposta (lista vazia) e precisou de ajuste.
  - **Com esta fatia, o Módulo 5 (Quiz) está 100% completo** — backend (Slices 1-4) e frontend (Slices 5-6), todos os QUIZ-RF-001 a 008/010 implementados. QUIZ-RF-009 (gate de assinatura) permanece a única pendência do módulo, deliberadamente adiada para o Módulo 6 desde a decisão de escopo da Slice 1.

- [x] 2026-07-24 — **Módulo 7, Fatia 0 — Infra pura greenfield da Geração de Questões por IA** (schema + os 4 componentes novos: PDF, LLM, storage R2, `SKIP LOCKED`), primeira das 6 fatias planejadas para o módulo. Nenhuma rota HTTP ainda — objetivo era provar isoladamente os componentes de maior risco antes das fatias de negócio (mesmo raciocínio de risco do scheduler isolado no Módulo 5).
  - **Schema**: tabelas `ai_reference_exams`, `ai_generation_jobs`, `ai_generated_questions` + enums `ai_generation_job_status`/`ai_review_status` em `schema.ts`, migration `0007_cynical_tyger_tiger.sql` gerada via `db:generate`.
  - **Decisão tomada com o usuário nesta sessão**: ADR-0022 revisada de `claude-sonnet-4-6` para **`claude-sonnet-5`** (já disponível, mais barato e de qualidade superior na data) — `ai-generation.md` e `decisoes.md` atualizados; modelo segue configurável via `ANTHROPIC_MODEL_AI_GENERATION`, não hardcoded.
  - **3 dependências novas** (nenhuma existia no projeto): `@anthropic-ai/sdk` (`infra/llm/anthropic.adapter.ts` — geração de questões via tool use forçado com `strict: true`, já que `output_config.format` não é portável entre modelos Claude; `cache_control: ephemeral` no bloco sistema+exemplos por ADR-0026), `unpdf` (`infra/pdf/pdf-text-extractor.ts` — escolhida sobre `pdf-parse`/`pdfjs-dist` puro por rodar bem em runtimes tipo Bun sem dependências nativas de Node), `aws4fetch` (`infra/storage/r2.adapter.ts` — assinatura SigV4 para R2 em ~3KB, evitando trazer `@aws-sdk/client-s3` inteiro só para 3 operações; mesmo espírito "fetch cru" do `CloudinaryAdapter`).
  - **Primeiro uso de `SELECT ... FOR UPDATE SKIP LOCKED` do projeto**: confirmado que `drizzle-orm@0.45.2` (já instalado) suporta nativamente via `.for("update", { skipLocked: true })`, sem precisar de `sql\`\`` raw. `claimNextPendingJob()` em `ai-generation-job.repository.ts` (só esse método nesta fatia — o repositório completo chega na Fatia 1).
  - **Achado de infraestrutura de teste**: o client de teste da aplicação é limitado a `max: 1` conexão de propósito (permite envolver cada teste em `BEGIN`/`ROLLBACK`), o que serializaria 2 chamadas concorrentes e não provaria nada sobre `SKIP LOCKED`. O teste de concorrência (`tests/e2e/ai-generation-job-claim.test.ts`) abre 2 clients Postgres ad-hoc próprios, fora do client compartilhado, para exercitar 2 transações genuinamente simultâneas — confirma que 2 conexões concorrentes nunca reivindicam o mesmo job `pending`.
  - **Novos mocks de HTTP** seguindo o molde de `mock-cloudinary.ts`/`mock-resend.ts`: `mock-anthropic.ts` (intercepta `api.anthropic.com/v1/messages`, sintetiza bloco `tool_use` + `usage.cache_read_input_tokens` configurável) e `mock-r2.ts` (intercepta `*.r2.cloudflarestorage.com`, armazena objetos em memória para round-trip PUT/GET/DELETE). `resetDatabase()` atualizado com as 3 novas tabelas.
  - **Verificação:** `bun run typecheck` limpo; `bun run test:unit` (104 pass, inalterado + 5 novos de `pdf-text-extractor`); testes novos de integração (`anthropic.adapter.test.ts`, `r2.adapter.test.ts`) e o e2e de concorrência do `SKIP LOCKED`, todos rodados individualmente (workaround de instabilidade do Bun/Windows) — todos verdes; regressão pontual (`archive-axis.usecase.test.ts`, `admin-axes.test.ts`) sem quebras.
  - **Smoke test com credenciais reais** (Anthropic + R2, script descartável fora do repo): achou 2 bugs reais no schema estrito da tool `generate_questions`, só visíveis contra a API de verdade (os mocks não validam schema) — `strict: true` da Anthropic rejeita `minItems`/`maxItems` diferentes de 0/1 em array (erro 400 em `alternatives`) e rejeita `minimum`/`maximum` em `integer` (erro 400 em `correct_index`). Corrigido: `correct_index` virou `enum: [0,1,2,3]` (suportado em strict mode); "exatamente 4 alternativas" saiu do schema (não expressável em strict mode) e foi para a `description` da propriedade, reforçado pela validação semântica em domínio na Fatia 3. Geração real confirmada (1 questão sobre extintores classe A, tokens registrados). R2 exigiu recriar o token — a primeira tentativa do usuário criou um token só leitura (`GET`/list funcionavam, `PUT` dava 403 `AccessDenied`); token recriado com **Object Read & Write** resolveu, round-trip upload/download/delete confirmado.
  - **Próximo passo**: Fatia 1 (`AIGEN-RF-001` — criar job, `POST /admin/ai-generation/jobs` multipart) usando os componentes desta fatia.

- [x] 2026-07-24 — **Módulo 7, Fatia 1 — Criar job de geração (AIGEN-RF-001)**, segunda das 6 fatias planejadas. Primeira rota HTTP do módulo.
  - **Domínio**: `domain/ai-generation/ai-generation-job.entity.ts` (invariante `question_count` 1-30, `InvalidQuestionCountError`), `ai-generation.errors.ts` (`AiGenerationJobNotFoundError`, `InvalidPdfContentTypeError`, `PdfCorruptedError`, `PdfHasNoExtractableTextError` — reservado para o worker da Fatia 3, `FileTooLargeError`, `DailyJobLimitExceededError`, `InvalidQuestionCountError`), `ai-generation-job.repository.port.ts` (`create`, `findById`, `countCreatedByAdminOnDate`, `countByStatuses`).
  - **Repositório Drizzle completo**: `DrizzleAiGenerationJobRepository` agora implementa a porta inteira, mantendo `claimNextPendingJob()` (Fatia 0) à parte da interface — segue sendo consumido diretamente pela linha crua na Fatia 3. Limite diário usa `to_char(created_at AT TIME ZONE 'America/Sao_Paulo', 'YYYY-MM-DD')`, mesmo padrão de `quiz-session.repository.ts` para agregações mensais.
  - **Use case** (`create-ai-generation-job.usecase.ts`): valida matéria ativa (reaproveita `SubjectNotActiveError` de `domain/content/question.errors.ts` — mesmo cross-agregado que `CreateQuestionUseCase` já fazia), valida os 2 PDFs (tamanho ≤20MB, assinatura `%PDF-`, estrutura via `UnpdfTextExtractor.extract()` descartando o texto — a checagem de texto extraível de fato é regra do worker, Fatia 3), checa limite diário de 10 jobs/admin, sobe os PDFs ao R2 **antes** de persistir o job (nenhum job "fantasma" sem arquivo se o upload falhar), grava `audit_log` com `action=create_ai_generation_job`.
  - **`queue_position`/`estimated_wait_seconds` (CA-4/CA-6)**: sem precedente nos RFs para o cálculo exato — implementado como heurística (não invariante de negócio): `queue_position = 0` se há slot livre (jobs `processing` < `AI_GENERATION_MAX_CONCURRENT_JOBS`) e nenhum `pending` na frente; caso contrário `pending_à_frente - slots_livres + 1`. `estimated_wait_seconds = queue_position × 90s` (duração média assumida, documentada em código como aproximação a ser refinada quando o worker real da Fatia 3 gerar dados de duração).
  - **Rota** `POST /admin/ai-generation/jobs`: multipart plano (`.post()`, não `.openapi()`) — mesmo padrão de `POST /admin/questions/:id/image`, primeiro upload binário do projeto. RBAC via `requireAuth`/`requireRole("admin")` no prefixo `/admin/ai-generation/*`. Retorna 202 com `{ job_id, status, queue_position, estimated_wait_seconds }`.
  - **Testes**: unit da entidade (`ai-generation-job.entity.test.ts`, invariante de `question_count` + imutabilidade de `with()`); e2e completo (`admin-ai-generation.test.ts`, 11 casos) cobrindo sucesso com upload real ao R2 stubado + audit_log, RBAC (403), campo ausente, matéria arquivada/inexistente, `question_count` fora do intervalo, arquivo >20MB (413), assinatura inválida e PDF corrompido (422 `invalid_pdf_file` nos dois casos), limite diário excedido (429), e o cálculo de `queue_position`/`estimated_wait_seconds` com 2 jobs `processing` + 1 `pending` semeados direto no banco.
  - **Verificação**: `bun run typecheck` limpo; suíte completa rodada arquivo-por-arquivo (workaround de instabilidade do Bun/Windows já documentado) — `test:unit` 109 pass (+5 desta fatia), todos os 19 arquivos de integração e todos os 22 arquivos e2e rodados individualmente, zero regressão; as únicas falhas observadas (`create-axis`/`create-question`/`create-subject`/`finish-quiz`/`get-quiz-result`/`review-question`/`start-quiz`/`submit-answer`, todas com "beforeEach/afterEach hook timed out") são o hang pré-existente já rastreado no Backlog, não relacionadas a esta fatia.
  - **Próximo passo**: Fatia 2 (`AIGEN-RF-002/003` — consultar status e listar histórico de jobs).

- [x] 2026-07-25 — **Módulo 7, Fatia 2 — Consultar status + histórico de jobs (AIGEN-RF-002/003)**, terceira das 6 fatias planejadas. Só leitura — sem worker (Fatia 3) nem revisão de questões (Fatia 4); jobs `completed`/`failed` com questões seguem semeados direto via repositório nos testes.
  - **Domínio**: `ai-generated-question.entity.ts` (entidade completa, não DTO solto — `review_status` já tem ciclo de vida real que ganha transições na Fatia 4; sem invariante de construtor nesta fatia, a validação semântica dos dados gerados fica para `parse-generated-questions.ts` na origem, Fatia 3) + `ai-generated-question.repository.port.ts` (`create`, `findByJobId` ordenado por `generation_index`, `countByReviewStatusForJobIds` agregado em lote). `ai-generation-job.repository.port.ts` ganhou `countPendingCreatedBefore(createdAt)` (queue_position recalculado para um job já existente, por FIFO real — diferente de `countByStatuses(["pending"])`, usado só na criação) e `list(filters)` paginado com join em `subjects`.
  - **Helper puro `calculateQueuePosition`** (`application/ai-generation/calculate-queue-position.ts`) extraído da fórmula que já existia inline em `create-ai-generation-job.usecase.ts` (Fatia 1) — mesma heurística, agora compartilhada com o `GetAiGenerationJobUseCase` novo, sem duplicar a lógica de `availableSlots`.
  - **Use cases novos**: `GetAiGenerationJobUseCase` (404 sem vazar existência entre admins — mesmo princípio de `GetQuizResultUseCase` do Módulo 5; `questions`/`summary` só populados fora de `pending`/`processing`, `summary` calculado em memória a partir do array já buscado) e `ListAiGenerationJobsUseCase` (sempre escopado a `createdBy=actor`, `summary` via agregação em lote para evitar N+1 entre os jobs da página).
  - **Rotas** `GET /admin/ai-generation/jobs/:id` e `GET /admin/ai-generation/jobs` via `.openapi()` (diferente do `POST` multipart cru da Fatia 1) no mesmo arquivo de rotas do módulo. `error_message` entra no item da listagem (AIGEN-RF-003 CA-5, confirmado no texto integral do RF — o JSON de exemplo do CA-1 é só ilustrativo).
  - **Verificação**: `bun run typecheck` limpo (nenhuma mudança de schema — tabelas já existiam desde a Fatia 0). Suíte completa rodada sem hang neste ambiente Linux: `test:unit` 116 pass (+7 desta fatia: `calculate-queue-position`, `ai-generated-question.entity`), `test:integration` 117 pass (+13: `get-ai-generation-job.usecase`, `list-ai-generation-jobs.usecase`), `test:e2e` 139 pass (+13 em `admin-ai-generation.test.ts`, 22 no total do arquivo — cobrindo pending/processing/completed/failed com resultado parcial, 404 cross-admin e para id inexistente, filtros multi-select de status via CSV, filtro por `subject_id`, `error_message` presente só em `failed`, 403 para não-admin, lista vazia). Smoke manual real via `curl` contra a API local (skill `run-bomberquiz-api`): job semeado direto via SQL (R2 real não configurado neste ambiente de sandbox — só placeholders em `.env`), consultado `pending` (`questions: []`, `summary: null`), depois marcado `completed` com 2 questões geradas e reconsultado (`summary` e `questions[]` corretos), e confirmado no `GET` de histórico.
  - **Achado de ambiente, não desta fatia**: `.env` local estava sem as 6 variáveis de Módulo 7 (`ANTHROPIC_*`/`R2_*`, sem default) — impedia `bun run dev` de subir. Preenchidas inicialmente com placeholders (não são segredos reais, `.env` é gitignored) para viabilizar o primeiro smoke test.
  - **Re-verificação com credenciais reais**: o usuário preencheu `ANTHROPIC_API_KEY`/`R2_*` reais em `.env` logo depois. Repetido o smoke test ponta-a-ponta com R2 de verdade: `POST /admin/ai-generation/jobs` com 2 PDFs reais → upload real ao bucket `bomberquiz-ai-generation-dev` → `GET /admin/ai-generation/jobs/:id` confirma `pending` (`questions: []`/`summary: null`) → job marcado `completed` com 2 questões semeadas (`review_status` `pending`/`discarded`) → `GET` novamente confirma `questions[]`/`summary` corretos, e `GET /admin/ai-generation/jobs` (com filtro `subject_id`+`status`) também reflete. PDFs removidos do R2 e linhas (job/questões/matéria/eixo/admin) removidas do Postgres por `id` exato ao final — sem `LIKE` desta vez.
  - **⚠️ Nota de limpeza pós-smoke-test**: ao limpar os dados de teste desta fatia, um `DELETE` em `audit_log` usou um filtro `LIKE 'delivered+admin%@resend.dev'` largo demais e removeu, além do admin criado nesta sessão, entradas de auditoria de **2 admins de teste de sessões anteriores** (dados de QA já existentes no banco de dev, não desta fatia). Nenhuma linha de `users`/`axes`/`subjects`/`questions` foi afetada, só o histórico de `audit_log` desses 2 registros antigos. Sem impacto funcional, mas registrado para transparência.
  - **Próximo passo**: Fatia 3 (`AIGEN-RF-004` — worker assíncrono, núcleo de negócio).

- [x] 2026-07-25 — **Módulo 7, Fatia 3 — Worker assíncrono (AIGEN-RF-004)**, quarta das 6 fatias planejadas, núcleo de negócio do módulo. Nenhuma mudança de schema (tabelas já existiam desde a Fatia 0) — trabalho 100% código de aplicação/infra.
  - **Domínio**: novo erro `NoValidQuestionsGeneratedError` (CA-7). `IAiGenerationJobRepository` ganhou `update(job)`, `findProcessingStartedBefore(cutoff)`, `markFailedIfStillProcessing(jobId, errorMessage, completedAt)` (update condicional `WHERE status='processing'` — evita corrida entre o worker principal e a varredura de timeout) e `claimNextPendingJob()` **passou a fazer parte da porta** (antes só existia na classe concreta, usada só por um teste avulso de concorrência; agora um use case real depende dela) e devolve a entidade já mapeada, não a linha crua do Drizzle. `IAiGeneratedQuestionRepository` ganhou `createMany` (batch insert). Novo `ai-reference-exam.repository.port.ts` — **sem entidade de domínio** (mesma razão de `audit_log`: escrita/leitura sem invariantes de ciclo de vida).
  - **Infra**: `DrizzleAiReferenceExamRepository`; os 3 métodos novos implementados em `ai-generation-job.repository.ts`; `createMany` em `ai-generated-question.repository.ts`. Ajuste cirúrgico em `anthropic.adapter.ts`: `questionCount` (recebido desde a Fatia 0 mas nunca usado) agora vira um segundo bloco de texto em `messages`, **fora** do bloco `system` cacheado — sem esse ajuste o modelo ignorava a quantidade pedida pelo admin.
  - **Funções puras novas em `application/ai-generation/`** (mesmo padrão de `calculate-queue-position.ts` da Fatia 2, não em `domain/`): `estimate-token-count.ts` (heurística ~4 caracteres/token), `identify-reference-questions.ts` (heurística por numeração — `1.`/`1)`/`QUESTÃO 01` — até 8 exemplos, CA-4), `split-material-into-chunks.ts` (empacotamento guloso por parágrafo, não fatiamento proporcional fixo — evita cortar no meio de frases, CA-6), `distribute-question-count.ts` (distribui `question_count` entre chunks), `parse-generated-questions.ts` (validação semântica pós tool-use — CA-7, a checagem de "exatamente 4 alternativas" que o schema estrito da Anthropic não conseguia expressar desde a Fatia 0), `build-system-prompt.ts` (monta o bloco cacheável **uma vez por job**, sem número de questão dentro — ver decisão de design abaixo).
  - **`ProcessAiGenerationJobUseCase`** (o worker, mesmo papel de `ExpireAndAbandonQuizzesUseCase` do Módulo 5): reivindica até `AI_GENERATION_MAX_CONCURRENT_JOBS` jobs por tick (transações curtas separadas, uma por reivindicação), processa todos concorrentemente via `Promise.allSettled` fora de transação, isola falha por job (não aborta o lote). Fluxo por job: baixa os 2 PDFs do R2 → extrai texto (CA-3, falha se algum vier vazio) → resolve cache de exemplos da prova de referência por SHA-256 (CA-4, hit/miss/sem-exemplos-reconhecíveis) → monta prompt uma vez → chunking se o material for grande (CA-6) → chama o LLM sequencialmente por chunk (mantém o cache de prompt dentro do TTL de 5 min) → valida questões (CA-7) → persiste em lote + finaliza job (CA-8) → remove os PDFs do R2 em qualquer desfecho. Falhas (CA-3/CA-7/CA-9) caem num catch único, gravam `audit_log` com `action=ai_generation_failed` (`actorId: null`, `actorRoleAtTime: "system"` — primeiro uso real desse valor de enum) e limpam o R2.
  - **`TimeoutAiGenerationJobsUseCase`** (CA-10): mesmo formato de loop com isolamento de erro por item. `PROCESSING_TIMEOUT_MINUTES = 5` é constante fixa de domínio (RF-004 CA-2/CA-10), não env var — `AI_GENERATION_TIMEOUT_JOB_CRON` controla só a cadência da varredura, não o limiar de staleness.
  - **Decisões de design**: (1) prompt caching × chunking — o bloco `[system + exemplos]` precisa ficar byte-idêntico entre chamadas de um job multi-chunk para haver cache hit (ADR-0026), por isso a contagem de questões por chunk vive num bloco separado não-cacheado dentro do próprio `anthropic.adapter.ts`; (2) concorrência "até 2 simultâneos" a partir de uma única cron com `protect: true` — o tick reivindica até `availableSlots` jobs e processa todos concorrentemente, o próximo tick só começa depois que o lote termina (aproximação aceita para single-machine, já documentada no plano do módulo); (3) `generation_index` sequencial sobre o array já filtrado (CA-7), não sobre a resposta bruta do LLM.
  - **Scheduler**: `src/index.ts` ganhou os registros `ai-generation-worker` (`AI_GENERATION_JOB_CRON`) e `ai-generation-timeout-sweep` (`AI_GENERATION_TIMEOUT_JOB_CRON`), construídos com `new` no mesmo padrão manual do job de expiração de quiz.
  - **Verificação**: `bun run typecheck` limpo. `test:unit` 149 pass (+33 desta fatia, 6 arquivos novos de funções puras), `test:integration` 129 pass (+12: `ai-reference-exam.repository`, métodos novos de `ai-generation-job.repository` incl. guarda de corrida do `markFailedIfStillProcessing`, `createMany` de `ai-generated-question.repository`), `test:e2e` 148 pass (+9: `process-ai-generation-job.usecase.test.ts` com 6 casos — sucesso ponta-a-ponta com R2/Anthropic stubados, CA-3 sem texto extraível, CA-7 zero questões válidas, CA-4 cache hit e cache miss, teto de concorrência com 3 jobs pendentes — e `timeout-ai-generation-jobs.usecase.test.ts` com 3 casos), zero regressão nas Fatias 0/1/2. `mock-anthropic.ts` estendido com um parâmetro `responses` para configurar retorno diferente por chamada (necessário para testar agregação multi-chunk).
  - **Smoke manual real** (créditos reais da Anthropic, autorizado pelo usuário): 3 jobs reais criados via `POST /admin/ai-generation/jobs` com PDFs pequenos de teste (manual de combate a incêndio + prova de referência), processados pelo worker real contra a API Claude de verdade. 2 de 3 completaram com sucesso (3 e 2 questões de boa qualidade, corretamente fundamentadas no material, `prompt_tokens`/`completion_tokens` preenchidos); 1 falhou com a mensagem exata do CA-7 (`"O modelo não retornou questões válidas..."`) — confirmado via chamada direta ao adapter que não era bug de parsing, e sim variação real e ocasional da resposta do modelo (exatamente o cenário que o CA-7 existe para tratar). **Bug real encontrado e corrigido durante o smoke**: `processing_started_at` e `completed_at` saíam sempre com o mesmo timestamp — o `now` capturado no momento da reivindicação (antes das chamadas de rede de 30-90s) estava sendo reaproveitado para `completed_at` também. Corrigido: `completedAt` (sucesso e falha) agora usa um `new Date()` fresco, capturado só no momento da escrita terminal. PDFs confirmados removidos do R2 real nos 3 casos (sucesso e falha); dados de teste (jobs, questões, matéria, eixo, admin) removidos do Postgres por `id`/`email` exato ao final, sem `LIKE`.
  - **Próximo passo**: Fatia 4 (`AIGEN-RF-005` a `008` — revisão de questões: editar/aprovar/descartar).

- [x] 2026-07-25 — **Módulo 7, Fatia 4 — Revisão de questões (AIGEN-RF-005 a 008)**, quinta das 6 fatias planejadas, fecha o ciclo de curadoria do módulo. Nenhuma mudança de schema.
  - **Domínio**: `AiGeneratedQuestion` ganhou invariante de construtor (mesma checagem estrutural de `Question.entity.ts`: exatamente 4 alternativas não-vazias/não-duplicadas, `correct_index` 0-3 — reaproveita `InvalidAlternativesError` do Módulo 3 em vez de duplicar a classe de erro) e 3 métodos de transição puros: `markEdited`, `approve`, `discard` — exatamente como reservado no comentário da Fatia 2. 4 erros novos: `AiGeneratedQuestionNotFoundError`, `AiGeneratedQuestionNotPendingError` ("Esta questão já foi revisada." — compartilhado por RF-005/006/007), `AiGenerationJobNotCompletedError` ("O job ainda não foi concluído."), `AiGenerationJobNoPendingQuestionsError` (RF-008). `IAiGeneratedQuestionRepository` ganhou `findById`/`update`.
  - **Use cases individuais**: `UpdateAiGeneratedQuestionUseCase` (RF-005, sem audit_log — CA-4 é explícito que edições intermediárias não geram entrada, só a aprovação final registra o estado definitivo), `ApproveAiGeneratedQuestionUseCase` (RF-006, reaproveita `IQuestionRepository` do Módulo 3 — cria `Question` `published` direto, mesmo padrão de `CreateQuestionUseCase`; `reviewed_by/reviewed_at/rejection_reason` da `Question` ficam `null`, o rastro de revisão fica em `ai_generated_questions`), `DiscardAiGeneratedQuestionUseCase` (RF-007, com `reason` opcional em audit_log). As 4 checagens comuns (job existe/pertence ao admin, job `completed`, questão existe/pertence ao job, questão `pending`) foram extraídas para `load-ai-generated-question-for-review.ts` — evita triplicar a mesma sequência de validação.
  - **Use cases em lote** (RF-008): `ApproveAllAiGeneratedQuestionsUseCase`/`DiscardAllAiGeneratedQuestionsUseCase` reaproveitam os use cases individuais **por composição** (injetados no construtor, chamados em loop) em vez de duplicar a lógica — funciona porque `txManager.run()` aninhado dentro de um `txManager.run()` já aberto não abre uma segunda transação real (mesmo comportamento confirmado na Fatia 3), então o lote inteiro roda como uma única transação (CA-1) com cada iteração isolada em seu próprio `try/catch` (erros de domínio capturados antes de qualquer escrita real do item, preenchendo `errors[]` sem abortar os demais — CA-5).
  - **Rotas** (5 novas, todas `.openapi()`, JSON puro): `PATCH .../questions/{qid}`, `POST .../questions/{qid}/approve`, `POST .../questions/{qid}/discard`, `POST .../approve-all`, `POST .../discard-all`. Reaproveita `actorFromContext`/`errorResponse`/`toGeneratedQuestionResponse` já existentes no arquivo de rotas.
  - **Bug real encontrado e corrigido durante a escrita da rota `PATCH`**: o handler construía o objeto `changes` sempre com todas as 5 chaves presentes (usando `undefined` para "campo não veio no corpo da requisição"); um merge ingênuo (`{...atual, ...changes}`) sobrescreveria os campos não editados com `undefined`, apagando-os. Corrigido filtrando explicitamente chaves `undefined` antes de mesclar, dentro do próprio `UpdateAiGeneratedQuestionUseCase` (não confiar que o chamador vai construir o objeto "certo"). Pego por teste de integração (edição parcial preservando os demais campos), antes de qualquer smoke manual.
  - **Verificação**: `bun run typecheck` limpo. `test:unit` 158 pass (+9 desta fatia: invariante + `markEdited`/`approve`/`discard` de `AiGeneratedQuestion`), `test:integration` 160 pass (+31: 5 arquivos novos, um por use case), `test:e2e` 166 pass (+40 em `admin-ai-generation.test.ts`: edição com 404/409/422/403, aprovação com criação real de `Question` `published` + audit_log + reflexo no `summary` do job, descarte com/sem `reason`, lote aprovar/descartar com contagens e 409 sem pendentes), zero regressão nas Fatias 0-3.
  - **Smoke manual real**: job `completed` com 4 questões `pending` semeado direto via SQL (sem gastar créditos da Anthropic — este fluxo não toca o worker). Ciclo completo via `curl` contra a API local: editar Q1 → aprovar Q1 (confirmado: nova linha em `questions` com `status=published`, aparece em `GET /admin/questions`) → descartar Q2 com `reason` → `approve-all` nas 2 restantes (Q3/Q4, `{processed:2, approved:2, errors:[]}`) → `GET` do job confirma `summary={pending:0, approved:3, discarded:1}` → `discard-all` numa fila já sem pendentes retorna 409 `no_pending_questions` corretamente. Dados de teste removidos do Postgres por `id` exato ao final (ordem respeitando FKs: `ai_generated_questions` → `questions` → `audit_log` → `ai_generation_jobs` → `subjects`/`axes` → sessão/conta/usuário do admin).
  - **Próximo passo**: Fatia 5 (desejável, `AIGEN-RF-009` — excluir job) ou início do plano de UI web para o Módulo 7 (ainda não fatiado — ver conversa de 2026-07-25).

- [x] 2026-07-25 — **Módulo 7, Fatia 5 (desejável) — Excluir job (AIGEN-RF-009)**, sexta e última fatia planejada do módulo. Backend do Módulo 7 fica **100% completo** (criar → consultar → processar → revisar → excluir); só resta o plano de UI web, ainda não fatiado.
  - **Migration real** (primeira desde a Fatia 0): `ai_generated_questions.job_id` deixou de ser `NOT NULL` (`0008_kind_blade.sql`, só `ALTER COLUMN ... DROP NOT NULL`). Necessário porque CA-3 exige que a linha de uma questão `approved` **sobreviva** à exclusão do job (preserva `reviewed_by`/`reviewed_at`/`edited` — o rastro de quem revisou), só perdendo o vínculo com o job apagado; a `Question` real em `questions` já estava protegida de qualquer forma pela direção da FK (`approved_question_id → questions.id`, apagar o filho nunca afeta o pai), mas sem o schema nullable a própria linha de `ai_generated_questions` também seria perdida.
  - **Domínio**: `AiGeneratedQuestion.jobId` virou `string | null`. 2 erros novos: `AiGenerationJobProcessingError` ("O job está em processamento. Aguarde a conclusão.") e `AiGenerationJobHasPendingQuestionsError` (mensagem dinâmica com o N de pendentes — não podia ser estático como os demais, CA-5 exige o número na mensagem).
  - **Repositórios**: `IAiGeneratedQuestionRepository` ganhou `nullifyJobIdForApprovedByJobId` (nulifica só as `approved`) e `deleteByJobId` (hard-delete do restante — `pending`/`discarded`); `IAiGenerationJobRepository` ganhou `delete`. `DeleteAiGenerationJobUseCase` chama os 3 nessa ordem exata dentro de um único `txManager.run()` — é a única ordem que não viola a FK (nulificar as aprovadas antes de apagar o resto e o próprio job).
  - **Limpeza best-effort do R2 fora do texto literal do RF**: jobs `completed`/`failed` já tiveram os PDFs removidos ao terminar (ADR-0024, Fatia 3), mas um job ainda `pending` (nunca processado) teria os 2 objetos vazados para sempre sem uma chamada explícita. Reaproveitado `fileStorage.delete()` (best-effort, nunca lança, já usado sem condicional em `markJobFailed`) incondicionalmente para os dois arquivos, fora da transação.
  - **Rota**: `DELETE /admin/ai-generation/jobs/:id`, `.openapi()`, corpo JSON `{ confirm_discard_pending?: boolean }` (mesmo padrão dos outros endpoints desta fatia que aceitam corpo opcional). Resposta `200` com `{deleted, approved_preserved, removed_questions}` (não `204` — há resumo útil a devolver, diferente de `DELETE /admin/questions/:id`).
  - **Verificação**: `bun run typecheck` limpo. `test:unit` 158 pass (sem novos — nenhuma invariante de entidade mudou). `test:integration`: novo arquivo `delete-ai-generation-job.usecase.test.ts` (+7: sucesso sem pendentes preservando a aprovada com `job_id` nulo, sucesso com `confirmDiscardPending`, 404 job inexistente/outro admin, 409 `processing`, 409 pendentes sem confirmação com N correto na mensagem, audit_log gravado). `test:e2e`: novo `describe` em `admin-ai-generation.test.ts` (+7: fluxo feliz com aprovação real prévia e verificação cross-tabela, 409 sem confirmação, 200 com confirmação, 404 inexistente/outro admin, 409 `processing`, 403 não-admin) — total 173 e2e, zero regressão nas Fatias 0-4.
  - **Smoke manual real**: job `completed` semeado direto via SQL com 3 questões `pending`; Q1 aprovada e Q2 descartada via `curl` real (não seed — para gerar uma `Question` `published` de verdade e testar a sobrevivência do vínculo), Q3 deixada `pending`. `DELETE` sem confirmação → `409` com mensagem `"Ainda há 1 questões pendentes de revisão..."` correto. `DELETE` com `confirm_discard_pending:true` → `200` com `{approved_preserved:1, removed_questions:2}`; confirmado via `psql` que o job sumiu (`GET` retorna 404), a linha de `ai_generated_questions` da aprovada sobreviveu com `job_id` `NULL`, a `Question` real permanece `published` (visível em `GET /admin/questions/:id`), e o `audit_log` de `delete_ai_generation_job` foi gravado com os campos corretos. Dados de teste removidos do Postgres por `id` exato ao final, em chamadas `psql` separadas (evita que uma falha de FK no meio de um lote multi-statement reverta as exclusões anteriores dentro do mesmo batch implícito).
  - **Módulo 7 (Geração de Questões por IA) está com o backend 100% completo.** Único item pendente do módulo é o plano de UI web (`bomberquiz-web`), ainda não fatiado.

- [x] 2026-07-25 — **Plano de implementação da UI web do Módulo 7 criado** em [`rf/ai-generation-ui-plano-implementacao.md`](rf/ai-generation-ui-plano-implementacao.md), fatiado em 6 partes (fundação/rotas → criar job → acompanhar job com polling → histórico completo → revisar questões → ações em lote + excluir job). Nenhuma fatia implementada ainda — só o roteiro, seguindo o mesmo formato do plano irmão do backend. Achados principais da exploração prévia (`bomberquiz-web`): rotas admin ficam sob `/painel/*` (não `/admin/*`, por causa do proxy do Vite); `schema.d.ts` do cliente OpenAPI tipado ainda não tem as rotas do módulo (regenerar é pré-requisito); `POST /admin/ai-generation/jobs` é multipart e precisa do padrão de `fetch` cru já usado no upload de imagem de pergunta; não existe precedente de polling nem de ações em lote/seleção múltipla no frontend — ambos serão convenção nova, introduzida seguindo os idiomas já usados no resto do projeto (React Query, `AlertDialog` inline, `sonner`).

- [x] 2026-07-25 — **UI web do Módulo 7, Fatia 1 — Fundação**, primeira das 6 fatias planejadas do frontend de Geração de Questões por IA.
  - **Pré-requisito (passo 0)**: `bun run openapi:generate` (contra a API local rodando) trouxe os 8 endpoints JSON do módulo pro `schema.d.ts` tipado (`delete` aparece na mesma entrada de path de `get`, `/admin/ai-generation/jobs/{id}`). `POST /admin/ai-generation/jobs` (multipart) segue de fora, como o upload de imagem de pergunta.
  - `features/ai-generation/ai-generation-api.ts` (novo) com `useAiGenerationJobs(filters)` — só a query de listagem por enquanto.
  - 3 rotas novas sob `/painel/geracao-ia` (histórico, `/novo`, `/:jobId`) + item "Geração por IA" em `PANEL_NAV_ITEMS`. `AiGenerationJobsPage` já lista jobs reais numa tabela (matéria/material/status/questões/criado em) com estado vazio; `/novo` e `/:jobId` são placeholders "Em construção" até as próximas fatias.
  - **Desvio do plano**: pulado o "teste de guard das 3 rotas" (redundante, `RequireAdmin` já é testado de forma agnóstica de rota em `require-admin-guard.test.tsx`) em favor de um teste de página real para `AiGenerationJobsPage` (lista populada + estado vazio + link "Nova geração"), mais valioso; `panel-layout.test.tsx` ganhou uma asserção pro 5º link de nav.
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 64 pass (+3 novos), zero regressão. Verificação visual real via skill `run-bomberquiz-web` (Playwright headless, admin de teste `delivered@resend.dev` criado no Postgres de dev): as 3 rotas renderizam corretamente — nav com o novo item ativo, lista vazia + botão "Nova geração", e os 2 placeholders navegáveis.
  - **Próximo passo**: Fatia 2 (`AIGEN-RF-001` — criar job: formulário com matéria, quantidade e upload dos 2 PDFs).

- [x] 2026-07-25 — **UI web do Módulo 7, Fatia 2 — Criar job (AIGEN-RF-001)**, segunda das 6 fatias planejadas.
  - `ai-generation-pdf-picker.tsx` (novo componente `AiGenerationPdfPicker`, extensão de `image-upload-field.tsx` para PDF — mesma checagem client-side de tipo/tamanho, "UX only"), reaproveitado 2x na página (prova de referência e material de estudo) em vez de um componente combinado — mais simples.
  - `features/ai-generation/schemas.ts` (novo): `createJobFormSchema` (matéria obrigatória, `question_count` inteiro 1-30). Os 2 PDFs ficam fora do Zod, geridos como `File | null` em `useState`.
  - `useCreateAiGenerationJob()` em `ai-generation-api.ts`: `fetch` cru multipart (mesmo padrão de `imageRequest` em `questions-api.ts` do Módulo 3), já que `POST /admin/ai-generation/jobs` fica fora do `.openapi()` do backend.
  - `AiGenerationNewJobPage` completa: Select de matéria (só ativas), input de quantidade, os 2 `AiGenerationPdfPicker`, bloqueio com toast se algum PDF faltar no submit, `navigate` pro detalhe com o `job_id` retornado (202).
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 68 pass (+4 novos), zero regressão. **Smoke manual real, com consentimento explícito do usuário sobre o custo** (mesmo protocolo da Fatia 3 do backend): formulário preenchido e submetido de verdade via Playwright contra a API local (worker incluso, rodando em background) — job criado, processado de verdade pela Anthropic (`claude-sonnet-5`, 1436+427 tokens), 1 questão gerada a partir do material enviado, job refletido como "Concluído" na lista real. Dados de teste (job + questão) removidos via `DELETE /admin/ai-generation/jobs/:id` ao final.
  - **Próximo passo**: Fatia 3 (`AIGEN-RF-002` — acompanhar job: polling enquanto `pending`/`processing`, visão de erro em `failed`).

- [x] 2026-07-25 — **UI web do Módulo 7, Fatia 3 — Acompanhar job (AIGEN-RF-002)**, terceira das 6 fatias planejadas.
  - `useAiGenerationJob(id)` em `ai-generation-api.ts`: `refetchInterval` condicional (3s enquanto `pending`/`processing`, para em `completed`/`failed`) — primeiro polling do projeto, sem precedente a seguir.
  - `useDeleteAiGenerationJob()` adiantado desta fatia (não da 6, como o plano original previa): a visão de `failed` já precisa de uma exclusão funcional. Implementado com o fluxo de 2 tentativas completo (409 → confirma → reenvia com `confirm_discard_pending: true`) — a Fatia 6 só vai reaproveitar este hook na lista.
  - `AiGenerationJobDetailPage` completa: 3 sub-visões por status (`pending`/`processing` com spinner + posição na fila; `failed` com mensagem real do backend + botão excluir; `completed` com resumo simples — a tabela de revisão fica pra Fatia 5); 404/erro tratados com link de volta ao histórico.
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 75 pass (+7 novos: fila, processando, falha+exclusão, 409 com segunda confirmação, concluído, 404, e um teste de polling real com fake timers confirmando a 2ª chamada após 3s). **Smoke manual real, com consentimento explícito do usuário**: job criado de verdade via Playwright, observado transicionando `Pendente` → `Processando` → `Concluído` na tela via polling real (sem reload), confirmado por screenshots em cada tick. Visão de `failed` testada com um PDF sem texto extraível (sem custo de Anthropic — falha antes de chamar o LLM); exclusão via UI confirmada ponta a ponta (diálogo → `DELETE` real → toast → navegação de volta ao histórico).
  - **Nota**: durante o smoke, um job real com um PDF de conteúdo genuíno (`NO-21-Emprego de Aeronave Remotamente Pilotada.pdf`) apareceu no Postgres de dev, criado fora deste teste (provavelmente uso manual da API local rodando em paralelo) — não foi tocado, só os jobs sintéticos criados para este smoke foram removidos.
  - **Próximo passo**: Fatia 4 (`AIGEN-RF-003` — histórico completo: filtros de status/matéria + paginação real).

- [x] 2026-07-26 — **UI web do Módulo 7, Fatia 4 — Histórico completo (AIGEN-RF-003)**, quarta das 6 fatias planejadas.
  - `AiGenerationJobsFilters` ganhou `status`/`subjectId` opcionais; `useAiGenerationJobs` agora envia `status`/`subject_id` na query (o schema tipado já tinha os 2 campos desde a regeneração da Fatia 1).
  - `AiGenerationJobsPage`: filtro de status e de matéria (`<select>` nativo, sentinel `"all"`, mesmo padrão de `questions-page.tsx`), paginação real com o componente `Pagination` compartilhado, nova coluna "Revisão" com 3 badges de resumo (aprovadas/pendentes/descartadas) quando `job.summary` existe, `—` quando `null`, e `error_message` de jobs `failed` exibido abaixo do material (AIGEN-RF-003 CA-5).
  - **Achado**: `summary` do backend não é exclusivo de jobs `completed` — é calculado para qualquer job em `FINISHED_STATUSES` (`completed` e `failed`), então um `failed` sem questões geradas mostra corretamente "0/0/0", não `—`. Comportamento correto do contrato, confirmado durante a verificação visual.
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 81 pass (+9 novos), zero regressão. Verificação visual via `run-bomberquiz-web` **sem custo de Anthropic** (só leitura): como o único job real do banco de dev pertence a outro admin (histórico corretamente escopado por `created_by`, CA-4), 2 jobs sintéticos foram inseridos direto no Postgres de dev (1 `completed`, 1 `failed` com `error_message`) só para exercitar os filtros e os badges contra a UI real — confirmados funcionando (status=failed e filtro de matéria cada um reduzindo a lista corretamente) — e removidos por ID exato ao final; o job pré-existente de outro admin não foi tocado.
  - **Próximo passo**: Fatia 5 (`AIGEN-RF-005 a 007` — revisar questões geradas: tabela + editar/aprovar/descartar individual na visão `completed`).

- [x] 2026-07-27 — **UI web do Módulo 7, Fatia 5 — Revisar questões geradas (AIGEN-RF-005 a 007)**, quinta das 6 fatias planejadas.
  - `features/ai-generation/schemas.ts` ganhou `editGeneratedQuestionFormSchema` e `discardGeneratedQuestionFormSchema` (motivo opcional, até 500 caracteres); `ai-generation-api.ts` ganhou `useUpdateGeneratedQuestion`/`useApproveGeneratedQuestion`/`useDiscardGeneratedQuestion`, todos invalidando o detalhe do job.
  - `ai-generation-question-edit-dialog.tsx` (fork simplificado de `question-form-dialog.tsx`, sem matéria/imagem/`resetStats`) e `ai-generation-discard-dialog.tsx` (fork de `reject-question-dialog.tsx`, motivo opcional em vez de obrigatório) — novos componentes.
  - `AiGenerationJobDetailPage`: sub-visão `completed` ganhou a tabela de revisão real (reaproveita `job.questions`, já vindo do próprio `GET /jobs/:id`) — badge de `review_status`, tag "(editado)", e Editar/Aprovar/Descartar só quando `pending`. Aprovar é direto, sem diálogo (mesmo fluxo do parceiro).
  - **Achado**: `question_data` é armazenado no Postgres em **camelCase** (`correctIndex`, `sourceReference`), não snake_case como a resposta HTTP — dados sintéticos inseridos com chaves snake_case causaram 500 silencioso na verificação (schema de resposta falhando ao validar `correct_index: undefined`). Corrigido inserindo com as chaves certas; documentado no plano de UI para não repetir.
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 85 pass (+4 novos), zero regressão. Smoke manual real via `run-bomberquiz-web` (sem custo de Anthropic — job sintético `completed` com 3 questões `pending` inserido direto no Postgres): editada uma questão (PATCH real confirmado), aprovada (confirmada publicada em `/painel/perguntas`), descartada outra. Job sintético, suas questões e a `Question` real criada pela aprovação removidos ao final por ID exato.
  - **Próximo passo**: Fatia 6 (`AIGEN-RF-008/009` — aprovar/descartar todas + excluir job na lista).

- [x] 2026-07-27 — **Bugfix backend: geração de questões falhando com `TypeError` críptico** (`api/src/infra/llm/anthropic.adapter.ts`), achado ao testar a Fatia 5 ponta-a-ponta com um job real (10 questões pedidas — bem abaixo do máximo de 30).
  - **Causa raiz**: `max_tokens: 8192` na chamada de tool use — baixo demais mesmo para 10 questões, já que `statement`/`explanation` podem ter até 2000 caracteres cada e as 4 alternativas até 500 cada (AIGEN-RF-005 CA-1). A resposta truncava antes de fechar o JSON do tool use; `toolUse.input` chegava sem a chave `questions`, e `parsed.questions.map(...)` explodia com `"undefined is not an object (evaluating '...questions.map')"` — um erro sem nenhuma pista da causa real, salvo cru em `error_message`.
  - **Correção**: `max_tokens` subiu para 16000 (folga generosa sobre o caso real observado, ainda abaixo do limiar que exigiria streaming, ~21333). Adicionado guard-rail: `response.stop_reason === "max_tokens"` ou `toolUse.input.questions` não sendo array agora lança `LlmResponseTruncatedError` (mensagem acionável: "tente novamente com uma quantidade menor de questões") em vez de estourar um TypeError opaco — mesmo padrão de erro definido ao lado do adapter concreto que `PdfStorageError` já usava em `r2.adapter.ts`.
  - Não é bug introduzido pela Fatia 5 (frontend) — o adapter não foi tocado nessa fatia; só ficou visível agora por ser o primeiro teste real de geração desde a Fatia 3. O aviso `Warning: TT: undefined function: 21` que aparece nos logs é ruído benigno do pdf.js (parsing de fonte embutida do PDF), não relacionado à falha.
  - **Verificação**: `bun run typecheck` e `bun run test:unit` (158 pass) limpos na API. Ainda não re-testado ponta-a-ponta com uma geração real (custo de Anthropic) — pendente confirmação do usuário para rodar de novo.

- [x] 2026-07-27 — **UI web do Módulo 7, Fatia 6 — Ações em lote + excluir job (AIGEN-RF-008/009)**, sexta e última fatia planejada do frontend. **Módulo 7 (Geração de Questões por IA) fica 100% completo (backend + frontend).**
  - `ai-generation-api.ts` ganhou `useApproveAllGeneratedQuestions`/`useDiscardAllGeneratedQuestions` (POST `.../approve-all` e `.../discard-all`, mesmo padrão de invalidação dos hooks individuais).
  - `AiGenerationJobDetailPage`: sub-visão `completed` ganhou os botões "Aprovar todas"/"Descartar todas" acima da tabela de revisão (só visíveis quando há questão `pending`), cada um com `AlertDialog` de confirmação e toast com a contagem processada.
  - `AiGenerationJobsPage`: nova coluna "Ações" com botão "Excluir" por linha, reaproveitando `useDeleteAiGenerationJob()` (já implementado na Fatia 3) e o mesmo fluxo de 2 tentativas (409 `ai_generation_job_has_pending_questions` → 2º diálogo de confirmação).
  - **Bug real encontrado e corrigido durante a verificação visual**: o 1º `AlertDialog` da lista usava `deletingJobId !== null` tanto para controlar sua própria visibilidade quanto como o ID a excluir; seu `onOpenChange` zerava `deletingJobId` sempre que o diálogo fechava — inclusive quando fechava só porque o 2º diálogo (pendências) estava abrindo, não por ação do usuário. Resultado: o 2º clique ("Excluir mesmo assim") sempre disparava com `id` indefinido, e a API respondia 404 "Job de geração não encontrado" em vez de excluir. Corrigido separando a visibilidade (`confirmDeleteOpen`, novo state) do ID-alvo (`deletingJobId`, agora só limpo em sucesso/erro final ou dismiss explícito do diálogo) — mesmo padrão já usado em `AiGenerationJobDetailPage` (lá `jobId` vem de `useParams`, imutável, por isso nunca teve esse problema). O teste unitário original não pegava o bug por checar só o `body` da chamada, não `params.path.id`; reforçado para checar os dois.
  - **Verificação**: `bun run typecheck` limpo. `bun run test`: 90 pass (+5 novos: aprovar/descartar todas na página de detalhe, botões ausentes sem pendentes, excluir direto e excluir com 2ª confirmação na lista), zero regressão. Verificação visual via `run-bomberquiz-web` **sem custo de Anthropic** (jobs sintéticos `completed` com questões `pending` inseridos direto no Postgres de dev): aprovar todas e descartar todas confirmados funcionando contra a API real (badges viram "Aprovada"/"Descartada", toast com contagem); exclusão sem pendentes confirmada (1 diálogo); exclusão com pendentes inicialmente reproduziu o bug acima (404 real, não só no teste), corrigido, e re-verificado ponta a ponta numa segunda rodada — DELETE com o ID correto, 200, job e questão realmente removidos do Postgres (confirmado por `SELECT` direto). Dados sintéticos (jobs, questões, matérias/eixos de apoio) removidos por ID exato ao final de cada rodada; nenhum dado de outro admin tocado.
  - Backend do módulo (Fatias 0-5, ver entradas de 2026-07-24/25 acima) e frontend (Fatias 1-6, ver entradas de 2026-07-25/26/27) agora ambos 100% completos — Módulo 7 encerrado.

- [x] 2026-07-27 — **Incidente de produção: `bomberquiz-api` crash-loopando por OOM ao criar job de geração com PDF real**, achado pelo usuário ao testar o ciclo anterior em produção (não relacionado ao código da Fatia 6, que ainda não tinha sido implantada).
  - **Sintoma**: frontend retornava "Não foi possível criar o job de geração"; logs do Fly mostravam `hyper error: connection closed before message completed`, falha do health check, e o Fly Doctor sinalizando "Machines Restarting a Lot" na máquina `2874de1b90d528`.
  - **Causa raiz**: `fly.toml` fixava a VM em **256MB de RAM** (`[[vm]] memory = "256mb"`). `CreateAiGenerationJobUseCase.validatePdf()` (`api/src/application/ai-generation/create-ai-generation-job.usecase.ts:130-138`) chama `unpdf` (baseado em pdf.js) para extrair texto dos 2 PDFs **de forma síncrona, dentro da própria requisição de criação do job**, antes de chegar ao worker. Com um PDF real (não os PDFs sintéticos pequenos usados nas verificações anteriores), o pico de memória do pdf.js estourou os 256MB e o kernel matou o processo Bun (`exit_code=137, oom_killed=true`, confirmado via `fly machine status`).
  - **Por que foi crítico**: a app roda em **1 única máquina** (`min_machines_running = 1`, sem réplicas) — o OOM derruba a API inteira (login, quiz, tudo) por ~1 min a cada tentativa, não só a geração por IA. Reproduziu 2x seguidas (13:27:23 e 13:28:13) enquanto o usuário tentava de novo.
  - **Mitigação imediata (stopgap)**: `fly.toml` `[[vm]] memory` de `256mb` para `512mb`, `fly deploy`. **OOM reproduziu de novo mesmo em 512MB** (2ª tentativa, 13:49:48) — dessa vez com `anon-rss:404652kB` (~395MB, contra ~152MB da 1ª vez), apesar dos 2 PDFs reais serem pequenos (140KB e 331KB). O usuário notou a inconsistência ("os pdfs são pequenos, será que o problema é o PDF ou é a forma que o processo está sendo criado?") — pergunta certeira que levou à causa raiz de verdade abaixo.
  - **1ª hipótese de causa raiz (parcialmente correta, insuficiente)**: `unpdf`'s `extractText(pdf, { mergePages: true })` (usado em `pdf-text-extractor.ts`) processa **todas as páginas do PDF em paralelo** via `Promise.all` internamente (`node_modules/unpdf/dist/index.mjs:341-347` e `367-377`). `UnpdfTextExtractor.extract()` foi reescrito para processar página por página **sequencialmente** (`for` + `await`), chamando `page.cleanup()` depois de cada página. `bun run typecheck` limpo, `bun run test:unit` 158 pass, `fly deploy` feito — **mas o OOM reproduziu de novo em produção (3ª vez, 14:23:38), com `anon-rss:405980kB`, praticamente idêntico à 2ª tentativa (~395MB)**. A correção da concorrência entre páginas era real e válida, mas não era a causa dominante deste incidente específico.
  - **Causa raiz de verdade, confirmada por reprodução controlada**: instrumentado `process.memoryUsage()` passo a passo e rodado contra os 2 PDFs reais (`NO-21.pdf`, `Prova2026.pdf`, cedidos pelo usuário) dentro de um container Docker com a **imagem de produção exata** (`api/Dockerfile`) e `--memory=512m` (replicando o ambiente Linux real, não o Windows local). RSS salta de ~39MB para **~533MB já na 1ª página do `Prova2026.pdf`**, junto com o aviso `Warning: TT: undefined function: 21` (fonte TrueType malformada/incomum embutida no PDF) — um comportamento **específico de Linux**: o mesmo arquivo processado localmente no Windows fica em ~220MB, sem o salto. `NO-21.pdf` sozinho não reproduz o problema. Ou seja, não é o tamanho do PDF nem a concorrência entre páginas — é uma interação específica entre essa fonte malformada e o pdf.js/Bun em Linux, que aloca uma quantidade grande e aparentemente fixa (~500MB) de memória ao tentar processar o glyph program dessa fonte.
  - **Achado relacionado, não corrigido nesta entrada**: `ProcessAiGenerationJobUseCase.processOneJob()` (`api/src/application/ai-generation/process-ai-generation-job.usecase.ts:102-105`) ainda processa os 2 PDFs **em paralelo** via `Promise.all` no worker (diferente de `CreateAiGenerationJobUseCase`, que já era sequencial) — mesmo padrão de risco, ainda não acionado porque nenhum job chegou a essa etapa durante o incidente (todos os crashes foram na criação do job, antes do worker). Fica no backlog.
  - **Confirmado que não houve gasto de tokens Anthropic**: extração de PDF sempre roda antes de qualquer chamada a `this.llm.generateQuestions()` no worker (linha 133, depois da extração nas linhas 102-108) — como todos os crashes aconteceram na criação do job (antes do job virar `pending` e o worker sequer pegá-lo), nenhuma chamada à API da Anthropic chegou a ser feita.
  - **Decisão final (custo vs. risco, do usuário)**: reproduzir corretamente exigiria consertar o parsing de fontes malformadas do pdf.js/unpdf (não trivial, biblioteca de terceiros) ou isolar a extração num subprocesso com limite de memória próprio (mais robusto, não implementado ainda). Dado o custo incremental de VM maior (256MB ~US$2,02/mês vs 512MB ~US$3,32/mês vs 1GB ~US$5,92/mês, tabela de preços Fly.io) e que o app ainda está em fase de teste pré-lançamento, o usuário optou por **reverter a VM para 256MB** (custo mínimo) e aceitar que a geração por IA com PDFs que acionem esse bug de fonte continua instável até uma correção de código real (subprocesso isolado ou tratamento específico do erro de fonte) ser implementada. `fly deploy` feito, confirmado 256MB ativo e health check passando.
  - **Pendência real (não resolvida)**: a extração de PDF ainda pode, em tese, matar a API inteira com o `Prova2026.pdf` (ou qualquer PDF com fonte problemática similar) em qualquer tamanho de VM testado até agora. Ver item de backlog abaixo para o encaminhamento (isolar extração em subprocesso com limite de memória próprio, para que o pior caso vire um erro tratado — "PDF não pôde ser processado" — em vez de derrubar a API para todos os usuários).

- [x] 2026-07-27 — **`ai-bot` — ferramenta local de geração de questões, separada do `bomberquiz-api`/`bomberquiz-web`.** Decisão de arquitetura tomada com o usuário como encaminhamento definitivo para o incidente de OOM acima (em vez de corrigir o parsing de fonte malformada do pdf.js, que é código de terceiro): mover todo o pipeline de PDF→Claude→revisão para fora do Fly.io, rodando localmente. Escopo deliberado: existe **só** em `ai-bot/`, nunca em produção; a barreira de acesso continua sendo o login real de admin contra o `bomberquiz-api` (`POST /auth/login`), o mesmo raciocínio de segurança de sempre — o código sozinho, sem credenciais válidas, não acessa nem altera produção.
  - `ai-bot/api` (Bun + Hono + Zod, sem banco/fila/worker próprios): `POST /session/login` (autentica contra `bomberquiz-api`, guarda o cookie de sessão só em memória do processo, nunca em disco), `GET /subjects` (proxy de `GET /admin/subjects?status=active`), `POST /generate` (síncrono: extração de PDF + geração via Claude, tudo em memória, sem persistir nada), `POST /publish` (proxy de `POST /admin/questions`, endpoint que já existia e já é usado pela tela manual de criar pergunta — **nenhuma mudança no `bomberquiz-api` de produção foi necessária**).
  - Lógica de geração duplicada verbatim de `api/src/infra/pdf/pdf-text-extractor.ts`, `api/src/infra/pdf/pdf-signature.ts`, `api/src/infra/llm/anthropic.adapter.ts` e as funções puras de `api/src/application/ai-generation/` (`build-system-prompt`, `parse-generated-questions`, `split-material-into-chunks`, `distribute-question-count`, `identify-reference-questions`, `estimate-token-count`), com testes unitários portados (27 testes, `bun test`). Deliberadamente **não** duplicado: tabela de jobs, worker/cron, fila, limite diário, upload R2, audit log, cache de `reference_exams` — tudo isso só existe por causa do modelo assíncrono, que deixou de existir aqui (geração roda síncrona, resultado direto na resposta HTTP).
  - `ai-bot/web` (Vite + React + Tailwind + shadcn/ui, mesmo stack de `bomberquiz-web` para reaproveitar componentes): 3 telas (login → gerar → revisar). `PdfPicker` copiado verbatim de `web/src/pages/admin/ai-generation-pdf-picker.tsx`; diálogo de edição adaptado de `ai-generation-question-edit-dialog.tsx` (mesma validação Zod, mas sem mutação de rede — edita estado local antes de publicar). Fala só com `ai-bot/api` local, nunca diretamente com o `bomberquiz-api`.
  - **Verificação feita**: `bun run typecheck` limpo e `bun run build` (Vite) OK nos dois projetos; suíte unitária das funções puras (27 testes) verde. **Verificação end-to-end real confirmada pelo usuário** (2026-07-27): login real, `Prova2026.pdf` (referência) + `NO-21.pdf` (material) — os mesmos 2 arquivos que causavam o OOM em produção — geraram 10 questões com sucesso rodando localmente, publicadas como rascunho via `POST /publish` → `POST /admin/questions`, e confirmadas visíveis na tela admin de produção. Sem nenhum crash, sem envolver o Fly.io. Usuário optou por manter as 10 questões (revisar e publicar depois) em vez de descartar como dado de teste.
  - **Pendência futura anotada, não decidida agora**: com o `ai-bot` validado em uso real, o pipeline de job assíncrono do Módulo 7 em produção (`create/process/get/list/delete-ai-generation-job`, worker, cron de timeout, telas de lista/detalhe/revisão do admin web) vira candidato a aposentadoria — reduziria superfície de manutenção e eliminaria de vez o risco de OOM em produção. Não fazer isso sem decisão explícita do usuário; por ora as duas implementações coexistem.

- [x] 2026-07-27 — **Aposentado o pipeline de job assíncrono do Módulo 7 em `bomberquiz-api`/`bomberquiz-web`**, mesmo dia da entrada acima — a "pendência futura anotada" foi decidida e executada ainda em 2026-07-27, mas essa execução nunca tinha sido registrada aqui (achado só agora, 2026-08-03, ao auditar o repo com `/graphify` — reforça o valor de conferir código real em vez de só ler a documentação).
  - **`api/`**: removidos por completo `src/domain/ai-generation/`, `src/application/ai-generation/`, rotas/schemas `admin-ai-generation.routes.ts`, `infra/llm/` (adapter Anthropic), `infra/pdf/` (extrator + assinatura de arquivo), `infra/storage/r2.adapter.ts`, os 3 repositórios Drizzle de IA, env vars (`ANTHROPIC_API_KEY`, `R2_*`), dependências (`unpdf` etc.) e todos os testes correspondentes — confirmado agora por `find`/`grep`: nenhum arquivo com "pdf", "llm", "anthropic", "r2" ou "ai-generation" resta em `api/src`. Migration `0009_little_sauron.sql` derruba as 3 tabelas (`ai_generation_jobs`, `ai_generated_questions`, `ai_reference_exams`) e os 2 enums associados — FK `ai_generated_questions.approved_question_id → questions.id` está no lado que foi apagado, então perguntas já aprovadas em `questions` não são afetadas.
  - **`web/`**: removidos `features/ai-generation/`, as páginas `pages/admin/ai-generation-*`, as 3 rotas `/painel/geracao-ia/*` e o item de navegação correspondente.
  - **Consequência para o achado de OOM registrado no Backlog** (PDF com fonte TrueType malformada, ver abaixo): deixou de ser só "mitigado" — o próprio código vulnerável (`unpdf`/pdf.js) não existe mais em `bomberquiz-api`, então o risco de derrubar a API de produção por esse caminho está eliminado, não só contornado.
  - **Reaproveitado sem remover**: `POST /admin/questions` (usado pelo `ai-bot` para publicar) e o novo `questions.source` (ver entrada abaixo) continuam intactos — a remoção foi só do pipeline assíncrono antigo, não de nada que o `ai-bot` ou o admin manual dependam.
  - **Verificação**: `bun run typecheck`, suíte completa (unit/integration/e2e) e `bun run build` limpos nos dois repos após a remoção; `web/src/lib/api/schema.d.ts` regenerado sem as rotas do módulo antigo.
  - **Nota de processo**: esta remoção deveria ter sido registrada no dia em que ocorreu, por convenção deste arquivo (ver `CLAUDE.md` § Convenções de trabalho). Documentando retroativamente para o arquivo não ficar divergente do estado real do código.

- [x] 2026-07-27 — **`questions.source` (`"manual" | "ai_bot"`)**, mesmo dia da remoção acima, adicionado para o painel admin distinguir perguntas geradas pelo `ai-bot` das criadas manualmente. Enum Drizzle `question_source`, default `"manual"` (migration `0010_magenta_arachne.sql`). `bot/api`'s `publishQuestion()` envia `source: "ai_bot"` explicitamente ao chamar `POST /admin/questions`; o painel web omite o campo e recebe o default `"manual"`. Tabela de Perguntas em `web/` ganhou coluna "Criação": `"{primeiro nome do autor} - {Manual|IA Bot}"`.
  - **Pendência para quando o Módulo 4 (Conteúdo Parceiro) for implementado**: hoje `POST /admin/questions` é `requireRole("admin")` só, então nenhum parceiro consegue enviar `source`. Quando o endpoint de submissão de parceiro existir, ele deve continuar defaultando/forçando `source: "manual"` — parceiro nunca deve conseguir setar `"ai_bot"` a partir de um campo de corpo não confiável (o admin/bot atual confia nesse campo porque é uma fronteira de confiança diferente, só admin autenticado).

- [x] 2026-08-03 — **Observabilidade: Sentry (erro) + telemetria de uso do PWA/quiz.** Motivado por QUIZ-P-01 (offline-sync do quiz) depender de "observação do uso real do PWA" — que não existia; resolvido junto com a tarefa em aberto de adotar Sentry (ver item abaixo, agora concluído), já que ambos cabem no free tier e reaproveitam a mesma sessão de trabalho.
  - **Sentry (frontend + backend)**: `@sentry/react`/`@sentry/node` integrados — `web/src/lib/monitoring/sentry.ts` (hooks em `global-error-handler.ts` e `route-error-boundary.tsx`) e `api/src/infra/monitoring/sentry.ts` (hook em `app.onError`, `infra/scheduler/scheduler.ts` e `infra/email/resend.adapter.ts`). Gated por `SENTRY_DSN`/`VITE_SENTRY_DSN` (opcionais, SDK não inicializa sem eles). `sendDefaultPii: false` + `beforeSend` mascarando cookies/headers de auth/e-mail (mesma disciplina do ADR-0031). Correlação frontend↔backend via o `request_id` já existente no envelope de erro. `web/vite.config.ts` ganhou `build.sourcemap: true` + `@sentry/vite-plugin` condicional (só ativa com `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` — nenhum desses é prefixado `VITE_` de propósito, pra não vazar pro bundle do cliente).
  - **Telemetria de uso (tabela própria, não SaaS de analytics)**: nova tabela `pwa_events` (Postgres/Neon, modelada sobre `audit_log` — mesma simplicidade, sem entity/errors de domínio dedicados), porta `IPwaEventWriter` (`domain/shared/pwa-event.port.ts`), `DrizzlePwaEventRepository`, `RecordPwaEventUseCase`, rota `POST /events` (`requireAuth` + `rateLimitByUser`). Allow-list de `event_type`: `pwa_install_prompted`, `pwa_install_accepted`, `pwa_install_dismissed`, `quiz_offline_period`. Frontend: `features/pwa/use-network-status.ts` (novo hook, escuta `online`/`offline`) e `report-pwa-event.ts` (fire-and-forget), instrumentados em `use-install-prompt.ts` e em `answer-quiz-page.tsx` (mede `duration_ms` de período offline durante quiz em andamento — a instrumentação que responde à pergunta original do QUIZ-P-01).
  - **Decisão de custo**: comparada explicitamente com PostHog antes de decidir — tabela própria escolhida por ser uma pergunta pontual (não analytics de produto contínuo), evitando fornecedor novo/peso no bundle/mascaramento adicional de PII em troca de ~150 linhas de código seguindo o padrão hexagonal já existente. R$0/mês adicional (Neon free tier já cobre o volume).
  - **Verificação**: `bun run typecheck` limpo nos dois repos; `bun run test` (67 testes frontend, incl. `install-banner.test.tsx` atualizado) e suíte completa do backend (unit/integration/e2e) verdes; migração (`0011_pale_galactus.sql`) aplicada em Postgres local; `POST /events` testado manualmente (401 sem auth) e via e2e novo (`tests/e2e/telemetry.test.ts`); tipos OpenAPI regenerados (`schema.d.ts`) incluindo `/events`.
  - **Pendências que dependem do usuário**: criar a conta/organização Sentry e configurar os DSNs como secret (Fly + Cloudflare Pages/GitHub Actions) para o monitoramento passar de "código pronto" para "ativo em produção"; configurar `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` no CI do `bomberquiz-web` para o upload de sourcemap funcionar.
  - **Concluído (2026-08-03, mesmo dia)**: contas Sentry criadas (projetos `bomberquiz-api` via SDK Bun, `bomberquiz-web` via SDK React — Tracing/Logs/Application Metrics/Session Replay/Crash Free Session deliberadamente deixados desligados, fora do escopo desta rodada). `SENTRY_DSN` configurado via `fly secrets set` nas duas apps; `VITE_SENTRY_DSN` configurado como *repository variable* (não secret — DSN não é sensível) via `gh variable set`; `SENTRY_AUTH_TOKEN` configurado como *repository secret* via `gh secret set`. Trocado `@sentry/node` por `@sentry/bun` no backend (SDK correto pro runtime real). Validado ponta-a-ponta: erro de teste disparado manualmente em cada app, confirmado aceito pelo Sentry (backend: visível na aba Issues; frontend: confirmada requisição real ao endpoint de ingest retornando `200` com `event_id`, diagnosticado via Playwright interceptando a rede — o contador da página de projetos do Sentry só demora/cacheia, não é bug da integração). **Bug encontrado e corrigido nesse processo**: `Dockerfile` fixa `NODE_ENV=production` nas duas apps Fly (produção e staging), então `environment: env.NODE_ENV` marcaria as duas como "production" no Sentry — corrigido derivando o ambiente de `FLY_APP_NAME` (injetada automaticamente pelo runtime do Fly) em vez de `NODE_ENV`; equivalente no frontend corrigido passando `--mode staging` no build de staging do `deploy.yml` (que sempre buildava em modo "production" antes). **Isso último ficou obsoleto no mesmo dia** — ver entrada abaixo sobre a aposentadoria do staging.

- [x] 2026-08-03 — **Aposentado o ambiente de staging** (Fly.io `bomberquiz-api-staging` + branch `staging` nos dois repos) — ver **ADR-0037** para o racional completo. Achado que motivou a decisão: revisão do estado real mostrou que a branch `staging` nunca foi criada em nenhum repositório desde a criação do app (2026-07-07) — o ambiente descrito em `arquitetura.md` nunca existiu operacionalmente, só o esqueleto (app Fly + steps condicionais no CI). Ações: `fly apps destroy bomberquiz-api-staging`; removido `api/fly.staging.toml`; simplificados `api/.github/workflows/deploy.yml` e `web/.github/workflows/deploy.yml` (gatilho de push só em `main`, sem mais steps condicionais `if: github.ref_name`); `arquitetura.md` atualizado (seção Ambientes, custos esperados, cliente HTTP). Pendências antigas sobre separar banco de staging/produção e atualizar `WEB_ORIGIN` do staging (ver acima) ficam obsoletas, não resolvidas.

- [x] 2026-08-03 — **Módulo 4 (Conteúdo Parceiro) iniciado — Slice 1: Listar + criar rascunho (PART-RF-001, PART-RF-002)**, primeira fatia de um ciclo planejado em 4 fatias backend (listar/criar → editar/enviar para revisão → excluir/imagem → histórico/dashboard) seguido de frontend, mesmo formato dos Módulos 3/5/7. Plano completo registrado antes de começar (ver `/home/andre/.claude/plans/rustling-finding-dragon.md`).
  - **Backend**: `ListOwnQuestionsUseCase` (`application/content/list-own-questions.usecase.ts`) — reaproveita `IQuestionRepository.list()` já existente (Módulo 3), forçando `authorId=self` e default de status `draft,pending_review,published` (CA-2). `CreatePartnerQuestionUseCase` (`create-partner-question.usecase.ts`) — **composição**, não duplicação: injeta o `CreateQuestionUseCase` do admin e só adiciona a checagem de limite de 50 rascunhos abertos (novo `IQuestionRepository.countDraftsByAuthorId`) antes de delegar, forçando `asDraft=true`/`source="manual"` no input (nunca aceita `source` do corpo — nota de segurança já registrada em memória sobre esse endpoint). Novo erro `TooManyDraftsError` (429). Rotas em `http/routes/partner-questions.routes.ts` (`GET/POST /me/questions`), usando `requireRole("partner")` — **primeiro consumidor real de RBAC no papel parceiro**, sem nenhuma mudança no middleware (hierarquia `client < partner < admin` já cobria exatamente a regra "admin também acessa").
  - **Achado de arquitetura, corrigido antes de implementar**: `IQuestionRepository.list()` só ordenava por `created_at` (hardcoded), mas PART-RF-001 CA-3 exige default `updated_at DESC`. Adicionado `sortBy?: "created_at"|"updated_at"|"published_at"` em `ListQuestionsFilters` (default `created_at`, preserva o comportamento de todo chamador existente — admin incluso) em vez de duplicar a interface só para o parceiro.
  - **Dois gaps de spec vs. realidade identificados e tratados como gap explícito** (mesmo padrão já usado em `subjects_count`/`total_answers` no Módulo 3): (1) PART-RF-001 CA-1 pede `submitted_at`, mas essa coluna só existe a partir da Slice 2 (migration planejada) — por ora a listagem sempre retorna `null`, documentado com TODO em `OwnQuestionSchema`. (2) PART-RF-001 CA-1 também pede `difficulty_level`, que depende de CONT-RF-017 (job de dificuldade, ainda no Backlog, fora do escopo do Módulo 4) — sempre retorna `"unrated"`, mesmo tratamento do gap pré-existente de `total_answers`/`accuracy` (sempre 0, nunca fechado desde o Módulo 3 apesar do Módulo 5/Quiz já existir — achado incidental, registrado aqui para não se perder, não corrigido nesta fatia por ser escopo de outro módulo).
  - **audit_log**: `create_draft_question` (ação distinta de `create_question_draft` do admin, per PART-RF-002 CA-3) — exigiu um pequeno `auditAction?: string` opcional em `CreateQuestionInput` para o `CreateQuestionUseCase` permitir esse override sem duplicar a lógica de gravação.
  - **Teste**: helper `registerPartnerUser()` (`tests/helpers/admin.ts`, criado na Slice 4 do Módulo 3 só como autor/destinatário de e-mail, sem cookie) ganhou `cookie` de login — primeiro uso real de login como parceiro nos testes. `test:unit` 99 pass (sem novos — nenhuma lógica de entidade nova), `test:integration` 112 pass (+7: `question.repository.test.ts` ganhou `countDraftsByAuthorId`/`sortBy`; 2 arquivos novos, `create-partner-question.usecase.test.ts` e `list-own-questions.usecase.test.ts`), `test:e2e` 127 pass (+8 em `partner-questions.test.ts`: fluxo completo, `source` do corpo ignorado, isolamento entre parceiros, 422 matéria arquivada, 429 no 51º rascunho, 403 client, 401 anônimo, 200 admin via hierarquia). Zero regressão nos módulos existentes.
  - **Próximo passo**: Slice 2 (PART-RF-003/004 — editar + enviar para revisão), que inclui a migration real de `submitted_at` (decisão já confirmada com o usuário durante o planejamento) e o primeiro uso real de `rateLimitByUser` (código morto desde sempre).

## Backlog (sem prioridade definida)

- [ ] **Módulo 3 (Conteúdo Admin) — concluído nesta rodada** (Eixos, Matérias, Perguntas+imagem, Fila de revisão — ver "Realizadas"). Pendências residuais específicas: badge `unread_review_events` do parceiro e link direto no e-mail de aprovação/rejeição (ambos dependem do Módulo 4 existir — ver entrada de 2026-07-20 acima).
- [x] ~~CONT-RF-017 (job de dificuldade) + scheduler in-process~~ — scheduler in-process implementado na Slice 2 do Módulo 5 (ver entrada de 2026-07-23 acima), mas só para o job de expiração de quiz; CONT-RF-017 em si continua adiado até haver dado real de resposta para validar o cálculo de dificuldade. Os outros 3 jobs (lembretes SUB-RF-007, purga de sessões AUTH-RF-008, dump de backup ADR-0020) reaproveitam a mesma infra (`infra/scheduler/scheduler.ts`) quando seus módulos existirem.
- [ ] **Investigar a causa raiz do hang de `beforeEach`/`afterEach` em testes de integração no Windows/Bun** (ver entrada de 2026-07-22 acima) — agora sabemos que correlaciona com testes que esperam rejeição (`.rejects`), não com arquivos específicos; ainda não sabemos o porquê. Confirmado na Slice 2 (2026-07-23) que não reproduz em ambiente Linux (suíte completa verde sem isolar arquivos). `test:unit` e `test:e2e` continuam confiáveis e são o caminho primário de verificação enquanto isso não for resolvido no Windows.
- [x] ~~Módulo 5 — Slice 6 (frontend histórico/desempenho/reset)~~ — concluída em 2026-07-24 (ver entrada acima). **Módulo 5 (Quiz) 100% completo**, exceto QUIZ-RF-009 (abaixo).
- [ ] **QUIZ-RF-009 (bloqueio de acesso por assinatura)** — deliberadamente não implementado na Slice 1 (ver decisão de escopo acima); implementar junto do Módulo 6, inserindo `requireActiveAccess` só em `POST /quizzes` e `POST /quizzes/:id/answers`.
- [x] ~~Módulo 7, Fatia 0 (infra greenfield: schema, Anthropic, R2, PDF, `SKIP LOCKED`)~~ — concluída em 2026-07-24 (ver entrada acima).
- [x] ~~Módulo 7, Fatia 1 (criar job — AIGEN-RF-001)~~ — concluída em 2026-07-24 (ver entrada acima).
- [x] ~~Módulo 7, Fatia 2 (consultar/listar — AIGEN-RF-002/003)~~ — concluída em 2026-07-25 (ver entrada acima).
- [x] ~~Módulo 7, Fatia 3 (worker assíncrono — AIGEN-RF-004)~~ — concluída em 2026-07-25 (ver entrada acima).
- [x] ~~Módulo 7, Fatia 4 (revisão de questões — AIGEN-RF-005 a 008)~~ — concluída em 2026-07-25 (ver entrada acima).
- [x] ~~Módulo 7, Fatia 5 (desejável) — excluir job (AIGEN-RF-009)~~ — concluída em 2026-07-25 (ver entrada acima). **Backend do Módulo 7 100% completo.**
- [x] ~~UI web (`bomberquiz-web`) do Módulo 7~~ — plano em [`rf/ai-generation-ui-plano-implementacao.md`](rf/ai-generation-ui-plano-implementacao.md), 6 fatias, todas concluídas (ver entrada de 2026-07-27 acima). **Módulo 7 (Geração de Questões por IA) 100% completo, backend + frontend.**
- [x] ~~Preencher credenciais reais de `ANTHROPIC_API_KEY`/`R2_*` em `.env`~~ — feito em 2026-07-24, smoke test real confirmou os dois adapters (ver entrada da Fatia 0 acima).
- [x] ~~PDF com fonte TrueType malformada ainda pode derrubar a API inteira por OOM~~ — **eliminado, não só mitigado** (atualizado 2026-08-03): o código vulnerável (`unpdf`/pdf.js processando a fonte malformada do `Prova2026.pdf` em Linux) não existe mais em `bomberquiz-api` — `infra/pdf/` foi removido por completo junto com o resto do pipeline antigo do Módulo 7 (ver entrada de 2026-07-27 "Aposentado o pipeline..." acima). Não é mais "mitigado por não rodar em produção"; o caminho de código em si foi apagado. Se o admin web tradicional for reintroduzido no futuro, o bug de origem (terceiro, pdf.js) voltaria a existir e precisaria ser reavaliado. `Prova2026.pdf`/`NO-21.pdf` seguem em `ai-bot/tmp/` para reprodução futura, caso necessário.
- [x] ~~`ai-bot` — verificação end-to-end real~~ — confirmada pelo usuário em 2026-07-27 (ver entrada acima): login real, geração real (10 questões, os 2 PDFs problemáticos), publicação real em produção, tudo funcionando ponta a ponta.
- [x] ~~Avaliar aposentar o pipeline de job assíncrono do Módulo 7 em produção~~ — **já executado em 2026-07-27** (ver entrada "Aposentado o pipeline..." nas Realizadas acima); este item ficou aberto no Backlog por engano, a decisão já tinha sido tomada e implementada no mesmo dia da criação do `ai-bot`. Corrigido em 2026-08-03 após auditoria do código real via `/graphify`.

- [ ] Atualizar webhook do Mercado Pago para `https://api.bomberquiz.com.br/webhooks/mercado-pago` quando a integração for desenvolvida (ver Fase 2 do domínio acima).
- [x] ~~Adotar Sentry (ou equivalente) de monitoramento de erro~~ — código integrado em 2026-08-03 (ver entrada acima), frontend + backend, PII mascarada, correlação por `request_id`. Falta só criar a conta/DSN (ação do usuário) para ativar em produção.
